# Candidatura Certa — status atual

**Atualizado em:** 22/09/2026
**Versão declarada da API:** 0.24.0  
**Git e produção:** a marca Candidatura Certa, o favicon, os contatos públicos, o chat de suporte e o Copiloto Chrome 1.0.1 estão publicados. O Render confirmou o deploy `c37363b` (“Prevent caching of HTML responses”) como **Live**; o transporte transacional Brevo HTTPS/SMTP foi implantado em `5a5eb0d` e segue incluído na produção. A matriz de portais/ZIP está publicada desde `1394cd6`. A extensão oferece análise contextual com consentimento e preenchimento assistido em seis portais; a submissão final permanece manual. A publicação na Chrome Web Store continua pendente.
**Produção:** `https://candidaturacerta.com.br` (o hostname Render continua disponível).
**Domínio próprio:** DNS resolve para a Cloudflare (`frida.ns.cloudflare.com`, `todd.ns.cloudflare.com`), o site responde por HTTPS e `APP_BASE_URL` está no Render. Os callbacks/allowlists dos provedores externos ainda precisam de revisão.
**E-mail de contato:** `contato@candidaturacerta.com.br` está publicado; MX do Zoho e SPF do relay Brevo foram verificados anteriormente. No Render, `BREVO_API_KEY` aparece salva e mascarada, assim como as variáveis `SMTP_*`. O deploy `5a5eb0d` está Live e usa o transporte compartilhado Brevo HTTPS/SMTP para recibos, entrega/reenvio de documentos e candidaturas por e-mail quando a chave existe. Os alertas de ciclo de vida, entrevistas e expiração também usam o transporte compartilhado Brevo HTTPS/SMTP; sua entrega real ainda não foi validada. A bateria focada passou em 56 testes; ainda falta um envio de documento em produção e a confirmação de recebimento. O Render Free não disponibiliza Shell.
**Repositório:** `usoparakotas21-boop/agente-de-candidaturas`  
**Diretório local:** `C:\agente_curriculos`

Este arquivo é o retrato operacional atual. O arquivo `PROJETO_STATUS.md` continua como histórico detalhado; quando houver conflito, este documento representa o estado mais recente.

## Pendências abertas e avanço

Os percentuais são por item, não devem ser somados como percentual geral do projeto. “Estimativa” identifica itens cuja porcentagem ainda não era acompanhada no histórico.

| Ordem | Item | Avanço | O que falta |
| --- | --- | ---: | --- |
| P0 | Segurança no escopo acordado | **100%** | Concluído; manter regressões quando novas rotas/conectores forem adicionados. |
| P1.22 — prioridade máxima do produto | Candidatura automática por canal autorizado | **40% (estimativa)** | Perfil, análise, documentos direcionados, histórico e Copiloto Chrome 1.0.1 autenticado, com botão persistente opcional por domínio, preparação, análise contextual da vaga, consentimento explicado em linguagem simples, sugestões Gemini com opt-in, cópia rápida e preenchimento consentido estão implementados e publicados. A versão estável 1.0.1 está Live no Render e distribuída por ZIP para instalação manual, agora com seletores para seis portais suportados pelo preenchimento assistido; a Chrome Web Store ainda não publicou o pacote. **Submissão automática continua em 0%:** não há conector oficial autorizado. Faltam API/convênio e autorização do empregador/ATS, além de consentimento por vaga/destino. O commit `58f973b` está Live e corrigiu os limites de taxa que causavam HTTP 500 nas rotas de reenvio, prévia e envio por e-mail. O commit `3bda5bd` está Live e adiciona suporte ao relay SMTP com TLS implícito e fallback opcional pela API HTTPS da Brevo; o complemento não envia formulários nem contorna CAPTCHAs ou proteções; para cada portal, o autofill só pode ser usado quando permitido. |
| P1.10 | Geração paga, recibo e entrega por e-mail | **92%** | `BREVO_API_KEY` aparece configurada e mascarada no Render; Brevo HTTPS é priorizada para recibos, documentos e candidaturas por e-mail, com SMTP como fallback. O Estúdio de Documentos agora guarda por 7 dias a candidatura que abriu o checkout e retoma a consulta/geração quando a pessoa volta à página. Os 56 testes focados de transporte, recibos, histórico de documentos, avisos e candidaturas por e-mail passaram, além dos 50 testes de retorno, geração e shell desta rodada. Falta a validação operacional do proprietário: em `/curriculos`, acionar “Tentar enviar por e-mail” em documento próprio ainda não enviado e confirmar chegada na caixa de entrada ou spam. O teste envia o currículo e a carta daquele registro para o e-mail confirmado da própria conta e não gera cobrança. A entrega real permanece sem comprovação.
| P1 | Assinaturas mensais Start e Pro | **90% (estimativa)** | Start R$ 34,90/mês e Pro R$ 99/mês, cobrança recorrente, webhook e cancelamento têm código; os 13 testes locais de assinatura passaram. Falta validar uma assinatura real e seu cancelamento. Nenhuma cobrança foi iniciada nesta rodada. |
| P1 | Bônus e publicação do e-book Hackeando DISC | **90% (estimativa)** | O arquivo está rastreado no Git; a rota libera o download para Pro e Consultoria ativos. Os testes agora cobrem Pro e Consultoria ativos, Start bloqueado, arquivo disponível e arquivo ausente. Falta confirmar o download em produção com uma conta Pro. A publicação editorial/distribuição do livro continua no lembrete de 22/09. |
| P1 | Consultoria recorrente | **90% implementação** | Checkout de R$ 197/mês, webhook idempotente, crédito de 1 atendimento por ciclo pago, solicitação de horário e cancelamento estão publicados no Render (`e00092e`). Falta uma compra controlada em produção e a primeira entrega real do atendimento. |
| P1.19 | E-mails de ciclo de vida | **90% (estimativa)** | Follow-up após 7 dias, avisos de entrevista e alertas de expiração mantêm opt-in explícito, outbox durável, retries, agrupamento e retenção de 60 dias. O dispatcher de expiração usa somente `JobPosting.validThrough`, revalida consentimento e data, deduplica por vaga/prazo e mantém a outbox server-only no RLS. Os três fluxos agora priorizam Brevo HTTPS e mantêm SMTP como fallback, inclusive TLS implícito na porta 465. A tela informa que expiração aguarda datas confiáveis das fontes oficiais. Resta validar recebimento real e qualidade das datas de origem. |
| P1.11 | Fechamento dos textos legais | **100% conteúdo publicado** | Termos e Política de Privacidade identificam Candidatura Certa Ltda., representante e endereço fornecidos pelo proprietário, e-mail e WhatsApp de privacidade, prestadores e retenção. A Política e os Termos foram conferidos em produção em 22/09/2026. Os Termos foram atualizados para descrever o envio por Brevo ou SMTP conforme o provedor configurado. Não foi feita validação independente de cadastro societário; o conteúdo reproduz os dados informados pelo proprietário.
| P1.18 | Suporte ao cliente | **90% (estimativa)** | Central de ajuda, WhatsApps e chat flutuante público/autenticado estão publicados. A base inclui dúvidas comuns, sete sugestões rápidas (incluindo Pix, biblioteca de documentos e cancelamento) e link para `/ajuda`; a resposta de planos informa corretamente que o e-book está incluído no Pro e na Consultoria ativos, não no Essencial ou Start; respostas conhecidas têm fallback local se Gemini retornar `unknown`. O launcher agora sobe quando o rodapé público ou autenticado entra na tela; `8e38a0a` está Live e o link do WhatsApp foi conferido visivelmente sem obstrução. Os testes focados do chat e shell cobrem os atalhos e o fallback. Os MX Zoho já resolvem publicamente; falta validar a entrega real das respostas/notificações pelo app e confirmar o canal ponta a ponta. |
| P1.6 | Prova operacional de exclusão LGPD | **100%** | Código e testes de portabilidade/exclusão concluídos; o proprietário confirmou a exclusão definitiva de uma conta de teste em produção. Não há bucket ativo. |
| P1.17 | Polimento visual residual | **100%** | Revisão visual autenticada concluída em produção. `4f23938` está Live no Render: launcher de suporte compacto sem flashes de CSS, resumo de extração em largura total e cabeçalhos duplicados removidos de Perfil, Configurações e Entrevistas. As telas foram conferidas visualmente; não há pendência bloqueante nesta etapa. |
| P1.21 | Simulador contextual e feedback de entrevista por IA | **90% (estimativa)** | Fluxo contextual, reinício seguro, validação/limites da resposta Gemini e fallback do roteiro para dados antigos ou malformados estão Live em `58d286a`, `b7bfe46` e `8c604fd`. `GEMINI_API_KEY` existe no Render, mas o valor ficou mascarado. A sessão autenticada disponível foi verificada em `/configuracoes` e está no plano Essencial. Faltam uma conta Pro autenticada e uma chamada real ao modelo para confirmar validade e fluxo ponta a ponta. |
| P1.20 | Governança legal/proveniência de fontes | **40% (estimativa)** | As nove fontes desejadas foram pesquisadas e registradas individualmente; **0/9 autorizadas**. O inventário dos 30 domínios ATS mostra 0/30 com autorização. O gate exige direitos de coleta e exibição comercial; faltam acordos/licenças reais para fontes piloto. |
| P1.1 | Métricas de resultado | **100% código** | Aguarda volume real suficiente; a interface evita conclusões antes do mínimo de candidaturas. |
| P2.1–P2.6 | APIs/ATS, ingestão, workers e observabilidade | **25% base técnica; 0% integração ativa** | Modelo de catálogo compartilhado, upsert idempotente, parser conservador, fila PostgreSQL com chave idempotente/lease/retry, worker e agendador local foram preparados e testados. A fila agora grava cada tentativa em `job_ingestion_runs`, com latência, contagens, erro sanitizado e resumo de saúde por fonte; quando a amostra mínima cai abaixo de 80% de sucesso, o worker bloqueia a tarefa antes do fetch e registra o motivo. O RLS mantém esses registros server-only. Nenhum serviço de coleta está ativo. O registro de fontes segue vazio e 0/9 fontes estão autorizadas. Faltam autorização/licença de uma fonte piloto, mapeamento do feed autorizado, configuração de serviços no Render, execução controlada e monitorização operacional real. |
| P2.7+ | Domínio próprio, Kanban, exportação e extensão de navegador | **95% configuração do domínio (estimativa)** | Nameservers e DNS do domínio raiz/`www` resolvem, o site está publicado por HTTPS e `APP_BASE_URL` está no Render. O Google usa o callback padrão do domínio; ainda falta permitir esse endereço no Google Cloud. O Render mantém o callback antigo do Outlook; cadastrar primeiro o novo endereço no Microsoft Entra e então trocar a variável do Render. Supabase e Mercado Pago também precisam de revisão final. O Copiloto Chrome 1.0.1 está publicado, distribuído por ZIP para instalação manual; o ZIP Live contém 18 arquivos e a matriz de seis portais; Chrome Web Store ainda não publicado. Kanban e exportação continuam no escopo de escala. |

### Atualização operacional — 22/09/2026

- **P1.22 — Copiloto Chrome 1.0.1:** o pacote bloqueia leitura e análise da página no LinkedIn, Glassdoor e Jobbol e oferece a alternativa de copiar manualmente o perfil, sem injetar scripts nesses domínios. A tela de instalação do Perfil aponta para o novo ZIP. Os 16 testes Node da extensão passaram. A publicação pública pela Chrome Web Store ainda depende do titular.

- **P1.10 — retomada do checkout (`22/09/2026`):** o Estúdio de Documentos grava apenas o ID da candidatura e um prazo de sete dias no armazenamento local do navegador quando abre o Mercado Pago. Ao retornar ou abrir novamente o estúdio, ele tenta restaurar a candidatura e consultar o pagamento; o registro é removido após falha definitiva ou geração pronta. Nenhum dado de currículo, documento, token ou pagamento é gravado no navegador. Os testes focados de retorno, geração e shell passaram `50/50`.

- **P1.10 — retorno à aba do checkout (`5be2431`, Render Live):** além do retorno do Mercado Pago e da reabertura do Estúdio, a aba original agora reconsulta o pagamento quando volta ao foco ou fica visível novamente. A consulta é limitada a uma tentativa a cada cinco segundos, mantém o escopo da candidatura do usuário e remove o marcador local quando a geração termina. A suíte focada permaneceu em `50/50`, o JavaScript inline passou em `node --check` e `git diff --check` ficou limpo.

- **Regressão após a retomada do checkout (`22/09/2026`):** a suíte oficial foi executada com as dependências de teste empacotadas e terminou em `437/437` testes aprovados. Os avisos são cenários simulados de indisponibilidade/rede e depreciações do runtime; não houve falha funcional.

- **Regressão completa mais recente (`22/09/2026`):** a suíte `unittest` foi executada novamente no ambiente virtual do projeto e terminou em `438/438` testes aprovados em `16,310s`. Os avisos registrados correspondem a cenários simulados de falha, indisponibilidade e depreciações; não houve falha funcional.

- **P1.20 — regressão parametrizada de governança (`22/09/2026`):** `tests/test_ats_registry_domains.py` foi executado com o pytest empacotado e terminou em `31/31` testes aprovados. A matriz de domínios e o bloqueio por falta de autorização permanecem íntegros; nenhuma fonte foi ativada.

- **P2.5 — saúde por fonte (`22/09/2026`, `a53b208` Live):** a fila passou a persistir uma linha server-only por tentativa em `job_ingestion_runs`, incluindo status (`succeeded`, `retry`, `failed` ou `blocked`), latência, contagens de registros e código de erro sem corpo bruto. `summarize_source_health` calcula amostra, taxa de sucesso, latência média, descartes e sinal de pausa após amostra mínima; o worker agora bloqueia uma tarefa antes do fetch quando esse sinal é acionado e registra `source_health_paused`. A migração e o RLS foram preparados para PostgreSQL. O Render confirmou o commit `a53b208` como **Live** e a regressão da fila desta extensão passou `8/8`; a regressão completa anterior terminou em `439/439` testes aprovados. Como o registro de fontes continua vazio, não houve coleta nem pausa operacional aplicada.
- **P0 — cache de documentos HTML (`22/09/2026`):** o middleware de segurança agora marca páginas HTML como `Cache-Control: private, no-store, max-age=0` e `Pragma: no-cache`, evitando que páginas públicas, autenticadas ou de checkout sejam reutilizadas por navegador ou proxy. Arquivos estáticos não recebem essa política forçada. O smoke test isolado do middleware passou; a suíte que importa `app.main` continua condicionada à dependência opcional `reportlab` disponível no ambiente de execução.
- **Regressão completa após o deploy de segurança (`22/09/2026`):** o ambiente `.venv` com as dependências do projeto executou `python -m unittest discover -s tests` e terminou em **441/441 testes aprovados** em 19,951s. Os avisos são cenários simulados de indisponibilidade, rede, MFA, Mercado Pago e depreciações; não houve falha funcional.

- **P1.10 — configuração declarativa do Brevo (`22/09/2026`):** `render.yaml` agora declara `BREVO_API_KEY` como entrada secreta `sync: false`, preservando a chave já cadastrada no Render sem colocá-la no repositório. A checagem de segurança do manifesto e a regressão focada passaram `60/60`.

### Atualização operacional — 21/09/2026

- **Deploy do status (`2891cf4`):** o Render confirmou o registro “Brevo key and Render configuration status” como **Live**. O repositório está limpo após o push e nenhum segredo foi incluído no commit.
- **Regressão ATS (`test_ats_registry_domains.py`):** a suíte parametrizada foi executada com as dependências pytest empacotadas em `tmp/pytest-deps` e passou `31/31`. A verificação confirma a matriz de domínios e os estados de governança registrados; não altera o gate de autorização, que continua exigindo evidência específica antes da coleta.
- **Validação geral após a auditoria do Render (`42b48fc`):** a suíte `unittest` foi executada com todos os módulos disponíveis, exceto o módulo parametrizado que depende de `pytest` ausente no runtime, e terminou com `413` testes aprovados em `15,421s`. A extensão Chrome passou `16/16` testes Node e `git diff --check` permaneceu limpo. Os avisos exibidos são cenários simulados de indisponibilidade/rede e não falhas de teste.
- **P1.19 — expectativa de alertas (`3b846dd`):** a opção de vagas expirando continua desativada até haver datas estruturadas confiáveis e envio configurado, mas o subtítulo agora explica isso em linguagem direta (“Em breve: aguardando validação de fontes oficiais e configuração do envio por e-mail”). Os 37 testes de UX/shell passaram.
- **P1.22 — auditoria do Copiloto 1.0.0 (21/09/2026):** a matriz modular continua cobrindo seis portais permitidos (Gupy, Vagas.com, InfoJobs, Catho, Empregos.com.br e Sólides). O pacote ZIP foi reconstruído e comparado por hash de cada arquivo interno com o artefato rastreado, sem divergências; portanto não havia mudança funcional nova para publicar. O painel lateral agora verifica o domínio antes de ler o DOM: LinkedIn, Glassdoor e Jobbol exibem um caminho de cópia manual sem injetar script ou extrair dados da página. A submissão final permanece deliberadamente manual e portais sem autorização continuam bloqueados.
- **P1.10 — transporte de e-mail (`caa00a5`):** SMTP completo agora vence uma chave Brevo residual no ambiente, e falhas preservam a etapa real (conexão, autenticação, envio ou API) sem registrar segredos. A regressão que classificava erro no envio como `connect` foi coberta; `67` testes Python focados, `16` testes Node da extensão, `32` testes de shell e `git diff --check` passaram. O envio real ainda depende da conectividade do relay ou da configuração opcional da API Brevo no Render.
- **P1.10 — evidência Brevo/Render (`21/09/2026`):** a conta Brevo autenticada agora mostra uma chave API ativa, exibida apenas de forma mascarada, além da credencial SMTP ativa. A variável `BREVO_API_KEY` não aparece na lista do Render após a conferência completa; por isso o fallback HTTPS ainda não pode ser ativado. A chave privada deve ser copiada pelo proprietário diretamente da Brevo para o campo `BREVO_API_KEY` no Render, sem passar pelo chat ou pelo Git. O registro anterior de “zero chaves API” é histórico e foi superado pela criação posterior da chave.
- **P1.10/P1.22 — reenvio de documentos:** o commit `58f973b` corrigiu o `KeyError: 'document-email-retry'` que dava HTTP 500, assim como os limites ausentes para prévia/envio de candidatura por e-mail. O commit `3bda5bd` está Live no Render e adicionou o caminho `SMTP_SSL` para a porta 465 e o fallback HTTPS da Brevo. O botão retornou HTTP 200, mas a tentativa controlada de `06:58:29` também expirou em `smtp_stage=connect` com `TimeoutError`, antes da autenticação; o log sanitizado registra somente fase, classe da exceção e código SMTP numérico opcional. Os testes não indicam senha inválida: a conexão de saída do Render ao relay não foi estabelecida. Para ativar o caminho HTTPS, basta cadastrar a chave privada da API da Brevo no Render como `BREVO_API_KEY`; ela não deve ser enviada ao chat ou ao Git. **31 testes focados e 413 testes `unittest` passaram**; o módulo pytest-only `test_ats_registry_domains.py` foi excluído porque o runtime do projeto nega a inicialização e o runtime alternativo não tem a extensão nativa necessária para `pytest`. Nenhum e-mail foi confirmado nesta tentativa.
- **P1.22 — Copiloto Chrome 1.0.0 (`1394cd6`, Render Live):** pacote estável reconstruído com 18 arquivos, incluindo o dicionário modular de seletores para seis portais. Listagem e nota de permissões/privacidade foram atualizadas; ZIP validado, instalação manual documentada e a versão publicada na página Perfil foi atualizada. `16/16` testes Node da extensão, sintaxe JS e `git diff --check` passaram. A distribuição estável está no ar pelo ZIP; ainda não foi enviada/publicada na Chrome Web Store.
- **P1.10/P1.19/P1.18 — SMTP:** revalidei o Render sem revelar valores; `SMTP_PASSWORD` aparece na lista de variáveis salvas e a Brevo mostra uma chave ativa mascarada, com último uso em 21/09/2026. Não substituí nem revelei a chave. O Render Shell não é suportado no plano Free. Na sessão autenticada de produção, `/curriculos` mostra um documento pago com o e-mail não enviado até confirmação do endereço da conta e um botão de reenvio. Nenhum segredo foi copiado para o chat. O teste final depende de confirmar esse endereço e então usar o botão de reenvio para verificar a chegada à caixa.
- **P1.22 — Copiloto 0.7.0 (`8f85d1c`, Render Deploy succeeded):** pacote local reconstruído com 13 arquivos. O painel lateral analisa título, empresa, local e descrição visíveis da vaga depois do consentimento, mostra score determinístico, pontos fortes/lacunas, experiência pertinente e resumo para copiar. A opção separada de Gemini começa desmarcada e envia somente campos profissionais minimizados; a análise não chama o modelo por padrão. A preparação contextual não grava o texto bruto do anúncio e não submete candidaturas. `406` testes Python e `14/14` testes Node da extensão passaram; sintaxe JavaScript e `git diff --check` passaram. `/health` não foi confirmado por HTTP nesta rodada porque o navegador bloqueou a navegação.
- **P1.22 — instalação mais simples (`f0c64ce`, Render Live):** a página Perfil agora explica em linguagem direta que a extensão só funciona no computador e ainda exige instalação manual fora da loja oficial. Os seis passos ficam visíveis, detectam Chrome/Edge para copiar o endereço correto da tela de extensões, explicam extração do ZIP e oferecem link de suporte. Os 32 testes de shell passaram e `node --check app/static/profile-autofill-export.js` e `git diff --check` passaram. Ainda falta publicação na loja oficial para remover o modo de desenvolvedor.
- **P1.22 — Copiloto 0.7.1 (`63e8feb`, Render Live):** versão do ZIP atualizada para 0.7.1 com 13 arquivos. O painel explica em palavras simples o que será comparado, esclarece que a empresa não recebe nada, mostra quais campos profissionais irão ao Gemini se a pessoa ativar essa opção e traduz o score/limites do mês. A análise não preenche a página nem envia candidatura. `15/15` testes da extensão, `32/32` testes de shell, checagem de sintaxe e verificação do ZIP passaram.
- **P2.7 — callbacks (`965dfbc`, Render Live):** o exemplo de configuração não aponta mais para um endereço genérico do Render; quando o campo fica vazio, Gmail e Outlook usam o domínio oficial automaticamente. `6` testes de URL/OAuth e `32/32` testes de shell passaram. No Render, `GOOGLE_REDIRECT_URI` não está definido (o padrão do domínio é usado); `OUTLOOK_REDIRECT_URI` ainda aponta para o host antigo. Não troquei essa variável porque a Microsoft precisa permitir o novo endereço primeiro. Próximo passo simples: cadastrar `https://candidaturacerta.com.br/auth/gmail/callback` no Google Cloud e `https://candidaturacerta.com.br/auth/outlook/callback` no Microsoft Entra; depois atualizar o callback do Outlook no Render.
- **Checkpoint P1 após `f31c3cc` (Render Live):** o domínio respondeu `health=ok`, `db=connected`; `/openapi.json` lista as seis rotas `/api/copilot/*`; `/static/candidatura-certa-autopreenchimento.zip?v=6` respondeu HTTP 200 com 22.298 bytes. O arquivo local confirma o pacote com 11 arquivos, incluindo `manifest.json`, service worker, content scripts, popup, painel lateral e README.
- **P1.22 — Copiloto 0.5.0:** autenticação da extensão pelo cookie de acesso da sessão do site, autorização explícita por portal, perfil owner-scoped, limite mensal no servidor, consulta de documentos privados, anexo após consentimento e painel lateral foram publicados. O complemento não submete candidaturas, não resolve CAPTCHA e não tenta imitar atividade humana. A versão da loja Chrome não existe; a distribuição atual é ZIP para instalação manual. `node --test tests/chrome-extension.test.cjs` passou **9/9** e os testes Python direcionados passaram **30/30**. O teste real em formulário ATS permanece pendente de uma sessão autenticada e de conferência pelo proprietário.
- **P1.10/P1.19/P1.18 — configuração SMTP atualizada:** a lista de variáveis no Render foi recarregada sem abrir valores. `SMTP_HOST`, `SMTP_FROM_EMAIL`, `SMTP_USERNAME`, `SMTP_PORT`, `SMTP_USE_TLS` e `SMTP_PASSWORD` aparecem salvos; a senha permanece mascarada. Portanto não foi feito envio real de recibo, documento ou ciclo de vida: a conta autenticada no site precisa confirmar o endereço antes do reenvio.
- **P1.21 — Gemini:** `GEMINI_API_KEY` aparece na lista do Render, mas não foi revelada nem usada para uma chamada de teste. A aba de produção `/dashboard` está no gate de autenticação, então não há conta Pro autenticada disponível nesta sessão para o teste ponta a ponta. Presença da variável não comprova validade da chave.
- **P1.22 — Copiloto 0.6.0 (Render Live em `8646f60`):** o complemento pode registrar um botão flutuante persistente somente no domínio ATS que a pessoa habilitar. O Chrome solicita acesso opcional ao host específico; desligar o recurso revoga o registro e a permissão. O detector lê URL e texto visível localmente e só revela dados do perfil após clique e consentimento. A revisão manual continua obrigatória. O pacote ZIP contém 12 arquivos e passou 11 testes do complemento, 14 testes Python de Copiloto/e-mail e verificações de sintaxe/manifesto. O painel do Render confirma o deploy **Live**. A validação HTTP direta ficou indisponível nesta rodada por bloqueio de saída do navegador/rede.
- **P1.22 — preparação de candidaturas no Chrome 0.2.1:** após a pessoa confirmar que o portal permite autofill e autorizar aquela página, o complemento MV3 preenche campos de texto reconhecidos e selects de cidade/estado apenas com opção exata do perfil. Usa `activeTab`, guarda o perfil apenas em `chrome.storage.local`, exige HTTPS, e não faz rede, login, upload, CAPTCHA ou envio. Bloqueia LinkedIn, Jobbol e Glassdoor conforme as políticas oficiais contra extensões/agentes automatizados. `node --test tests/chrome-extension.test.cjs` passou **7/7**; a submissão automática continua em **0%** até existir API/convênio oficial e autorização documentada por ATS/empregador.
- **P1.22 — viabilidade dos conectores revisada em fontes oficiais:** LinkedIn proíbe extensões de terceiros automatizando atividade no site; Indeed exige integração de parceiro sob os termos da API; Lever precisa de uma chave da conta empregadora com permissão do endpoint; e a API de criação de aplicação da Gupy exige token de recrutamento da empresa e, para candidatos inseridos manualmente, convite aceito pela própria pessoa. Nenhuma dessas credenciais é uma autorização geral vinculada à conta do candidato. Referências e escopo documentados em `SOURCE_GOVERNANCE.md`.
- **P1.19 — datas de validade de vagas:** o importador da vaga individual lê somente o `validThrough` explícito do JSON-LD `JobPosting`, normaliza fuso/data sem inferir prazo a partir do texto e armazena/exibe a data quando disponível. Testes cobrem prazo válido e dados ausentes/inválidos; vagas antigas continuam sem prazo. O dispatcher agora usa apenas essa data estruturada, conforme detalhado no item seguinte.
- **P1.19 — dispatcher de expiração implementado:** o worker envia um aviso opcional, agrupado pela frequência escolhida, apenas quando a vaga traz `valid_through` estruturado e ainda válido. Consentimento sem data válida/futura é recusado; opt-out, mudança de prazo ou frequência são revalidados antes do envio. A outbox guarda só identificadores/prazo, deduplica por usuário/vaga/prazo, limita tentativas, recupera leases vencidos, usa o e-mail confirmado do Supabase e é removida após 60 dias. RLS está habilitado sem política de leitura pelo cliente; portabilidade e exclusão da conta incluem a outbox. Os testes cobrem deduplicação, mudança de data, expiração sem fonte explícita, consentimento, endereço confirmado, não verificado, SMTP ausente, retries e retenção. Envio real permanece pendente: `SMTP_PASSWORD` agora aparece salva no Render; falta confirmar o endereço da conta e comprovar entrega.
- **P1.22 — prioridade máxima confirmada:** candidaturas efetivamente enviadas em nome da pessoa são o objetivo principal do produto, mas continuam desligadas. O próximo avanço implementável depende de um empregador/ATS conceder endpoint oficial, credencial e autorização que cubram o envio; a pessoa candidata também precisa confirmar cada destino/vaga. Até existir esse primeiro acordo, a experiência publicada prepara a candidatura e abre o portal para envio manual. Nenhuma autorização foi inferida das integrações de busca.
- **P1.17 encerrado (`4f23938`, Render Live):** conferi em produção as páginas autenticadas de Dashboard, Vagas, Candidaturas, Currículos, Criar documentos, Perfil, Entrevistas, Configurações e Segurança. Corrigi o flash de estilo inicial e a sobreposição do chat, deixei o resumo do OCR de Currículos em largura total e removi cabeçalhos redundantes. Os 43 testes direcionados de shell/chat passaram; `node --check app/static/support-chat.js` e `git diff --check` também passaram.
- **P1.20 — nove fontes prioritárias e gate endurecido:** LinkedIn, InfoJobs, Vagas.com, Sólides, Jobbol, Catho, Gupy, Empregos.com.br e Glassdoor foram pesquisados e registrados com decisão por fonte em `SOURCE_GOVERNANCE.md`; nenhuma das nove tem autorização arquivada. Os 30 domínios conhecidos pelo classificador têm estado explicitamente bloqueado. A verificação padrão exige direitos para coleta automática e exibição comercial; usos adicionais também devem estar no escopo. Os 15 testes de governança e três verificações diretas de domínio passaram; o módulo parametrizado de ATS depende de `pytest`, ausente no runtime disponível. Nenhuma coleta automática foi iniciada.
- **P1.22 — preparação automática da candidatura:** em `/vagas`, o link “Preparar candidatura com IA” passa o ID da candidatura e aciona a prévia personalizada ao abrir o estúdio; o usuário ainda revisa os documentos e conclui o envio no portal. URLs externas aceitam apenas HTTP/HTTPS sem credenciais. A sintaxe JavaScript e `git diff --check` passaram; 14 testes de governança e três verificações diretas de domínio também passaram. O Render confirmou `87f542f` **Live**. A conferência visual/autenticada da tela não foi possível nesta rodada: o navegador de automação bloqueou a URL do app e a aba disponível está deslogada. Nenhum conector oficial de submissão nem autorização de portal está registrado.
- **P1.22 — gate específico de envio automático (`81adcf1`, Render Live):** `SourceUse.AUTOMATED_SUBMISSION` é um direito independente no gate de governança; os 15 testes confirmam que aprovações de coleta/exibição não liberam o envio e que somente autorização explícita permite essa finalidade. As APIs oficiais consultadas são integrações de parceiros/empregadores: LinkedIn Apply Connect exige aprovação de parceiro; Indeed Send Candidates é uma integração para parceiros; Lever Apply exige chave da conta empregadora com permissão do endpoint. Isso não habilita candidaturas gerais nas contas dos usuários sem adesão dos empregadores/portais. Este gate é uma salvaguarda para futuros conectores; como ainda não existe um conector de submissão, nenhuma candidatura é enviada automaticamente.

- **Checkpoint posterior — 21/09/2026:** o Cloudflare agora mostra os MX Zoho `mx.zoho.com` (prioridade 10), `mx2.zoho.com` (20) e `mx3.zoho.com` (50); `Resolve-DnsName` contra 1.1.1.1 confirmou os três. A caixa de entrada ainda não recebeu um e-mail de teste nesta validação. O Render mostra `SMTP_FROM_EMAIL`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USE_TLS` e `SMTP_USERNAME`, e `SMTP_PASSWORD` não aparecia naquele checkpoint (a conferência atual confirma a variável salva); nenhum valor secreto foi aberto ou copiado.
- `25c813e` reservou espaço inferior no rodapé público. A validação em largura estreita mostrou que o launcher ainda podia cobrir o WhatsApp; `8e38a0a` agora o desloca acima dos rodapés público e autenticado usando a interseção do rodapé com a tela, e ajusta o painel aberto para caber acima da área reservada. O Render confirmou **Live**; no domínio público, o launcher e o link de WhatsApp ficaram visualmente separados. `tests.test_support_chat` e `tests.test_shell` passaram **43 testes**, assim como `node --check app/static/support-chat.js` e `git diff --check`.

- O envio de recibos, documentos e notificações agora só é ativado quando `SMTP_HOST`, `SMTP_FROM_EMAIL`, `SMTP_USERNAME` e `SMTP_PASSWORD` estão preenchidos; as três rotas usam a mesma validação. Isso mantém a aplicação em estado “não configurado” enquanto a chave Brevo não for inserida, em vez de tentar login vazio. Foram adicionadas regressões para notificações gerais, recibos do Mercado Pago e entrega de documentos; **46 testes SMTP/recibos/documentos/notificações passaram**, além dos **9 testes do chat**. Esse registro precede o save atual: a senha não foi aberta nem copiada, e `SMTP_PASSWORD` agora aparece salva no Render.
- Após o commit `7891502` ficar **Live**, foram salvos no Render os valores `SMTP_HOST`, `SMTP_FROM_EMAIL`, `SMTP_USERNAME`, `SMTP_PORT=587` e `SMTP_USE_TLS=true`. A senha não foi visualizada nem digitada; `SMTP_PASSWORD` aparece salva na conferência atual. O SPF do Brevo foi publicado. O MX era nulo no checkpoint inicial; ver o checkpoint posterior nesta seção para a configuração Zoho atual.
- A revisão autenticada de `/configuracoes` encontrou o contador de cota excedida sem explicação; `configuracoes.html` agora informa que os itens salvos permanecem acessíveis, a data da renovação e as opções de plano. Em `/vagas`, o separador de metadados junto ao badge de compatibilidade fazia um score positivo parecer negativo. A serialização das rotas de vagas, candidaturas e entrevistas também limita scores inválidos a 0–100, e os badges usam faixas verde/âmbar/neutra. Nada é regravado no banco. `d6ab263` acrescentou cache-buster para o script e retirou o separador do badge; o Render confirmou o deploy **Live** e a página autenticada mostrou 86% e 79% sem sinal negativo.
- `e00092e` habilitou a assinatura recorrente da Consultoria: a página pública exibe o botão **Assinar Consultoria**, o Mercado Pago recebe a frequência mensal e o valor fixo de R$ 197,00, o webhook libera 1 atendimento por ciclo pago e a pessoa solicita horário em Configurações com encaminhamento ao WhatsApp dedicado. O Render confirmou o deploy como **Live** e a landing pública confirmou o botão e o preço; a compra real e a entrega da primeira sessão continuam como evidência operacional pendente.
- `979c5c5` publicou a marca **Candidatura Certa**, o favicon SVG exclusivo, contatos atualizados na landing page, ajuda, configurações, páginas legais e comprovante, e links separados para suporte e consultoria. `cf315b3` acrescentou o chat público/autenticado; o endpoint responde usando Gemini apenas para classificar a pergunta e seleciona respostas revisadas, com fallback local e rate limiting. A landing em produção e o chat foram verificados.
- A delegação do domínio `candidaturacerta.com.br` resolve para `frida.ns.cloudflare.com` e `todd.ns.cloudflare.com`; o apex e `www` apontam para o Render. HTTPS, landing, planos e chat foram confirmados. `APP_BASE_URL` está aplicado; faltam callbacks/allowlists externos. Os registros MX Zoho foram adicionados depois do checkpoint inicial; consulte a atualização operacional acima. Para envio pelo aplicativo, `SMTP_PASSWORD` está salva; o teste de entrega aguarda a confirmação do e-mail da conta.
- `8e9b54a` ampliou a Central de Ajuda e foi confirmado **Live** no Render. A revisão em produção confirmou as respostas sobre renovação mensal/cancelamento, biblioteca/reenvio de documentos e preparação contextual para entrevista.
- O widget `/static/support-chat.js` e CSS são carregados na landing e no shell autenticado. A API limita requisições, sanitiza a entrada, usa Gemini para classificar temas permitidos e responde com conteúdo revisado; o cliente usa `textContent`. Em 21/09, perguntas rápidas e respostas foram confirmadas em produção. O MX Zoho já resolve; a entrega à caixa e respostas de e-mail pelo aplicativo seguem sem teste porque o teste ainda aguarda confirmação do e-mail da conta; `SMTP_PASSWORD` aparece salva.
- Registro histórico anterior ao último save do Render, em 21/09: a auditoria de nomes encontrou apenas `SMTP_PORT` e `SMTP_USE_TLS`; nenhum segredo foi aberto nem copiado. O save posterior adicionou `SMTP_HOST`, `SMTP_FROM_EMAIL` e `SMTP_USERNAME`; naquele registro faltava `SMTP_PASSWORD`; a conferência atual confirma que a variável aparece salva.
- Em 21/09, a conta Brevo mostrou `candidaturacerta.com.br` autenticado e relay SMTP ativo; a chave permaneceu mascarada. O SPF continua `v=spf1 include:spf.brevo.com ~all`. Depois, o MX Zoho foi adicionado e resolvido publicamente; falta teste de recebimento e envio.
- A política pública de privacidade agora descreve os fornecedores ativos e suas finalidades, os dados enviados ao Gemini, a verificação de senhas por prefixo de hash, retenção atual de mensagens/documentos por até 60 dias e backup criptografado no GitHub Actions por até 30 dias. Foram adicionados testes para preservar essas divulgações. A identificação civil/jurídica do controlador ainda depende de informação do proprietário.
- Na validação compatível mais recente, **363 testes unittest passaram**. `test_ats_registry_domains.py` depende de `pytest`, indisponível neste runtime; a execução separada anterior registrou 29/29. `node --check` passou nos scripts de Configurações, vagas e chat, assim como `py_compile app/main.py` e `git diff --check`.
- Na publicação anterior, `/privacidade` retornou HTTP 200 com divulgações de fornecedores, IA e retenção. O chat aceita `message` e `question`, inclui respostas revisadas, perguntas rápidas e fallback local para `unknown`; agora a suíte específica tem 12 testes aprovados. A cota excedida de Configurações está em `77f7a77`; a biblioteca explica armazenamento e bloqueia o envio de documentos por e-mail sem endereço confirmado.
- A inspeção da landing em `/#planos` confirmou os botões Start, Pro e Consultoria alinhados. A revisão autenticada de 21/09 foi concluída em Dashboard, Vagas, Candidaturas, Currículos, Criar documentos, Perfil, Entrevistas, Configurações e Segurança. O Render confirmou `4f23938` Live; a conferência visual confirmou o resumo de extração em largura total, launcher compacto sem sobreposição e um único cabeçalho global nas subpáginas.
- A migração RLS de produção foi reaplicada de forma idempotente para cobrir `consultation_credits`. A verificação no catálogo PostgreSQL encontrou as 17 tabelas do modelo, sem tabela ausente ou RLS desabilitado; as políticas de propriedade e a outbox interna preservam o isolamento previsto. Nos módulos direcionados de autenticação, shell, URLs públicas, RLS/IDOR, chat, IA e controles de segurança, **94 testes passaram**; `node --check` também passou para o JavaScript do widget.
- `b7bfe46` endureceu a integração Gemini: respostas acima de 64 KiB são rejeitadas antes do parse, a geração limita a saída a 2048 tokens, o score deve ser numérico/finito e todos os campos de texto/listas têm limites. `8c604fd` protege o roteiro contextual contra JSON armazenado fora do formato e limita gaps/pontos fortes. Ambos foram confirmados **Live** no Render.
- A verificação anterior desta rodada passou **327 testes** e os dois testes Node do reinício/troca de contexto; a validação mais recente, registrada acima, passou 353 testes Python. Ainda falta validar o Gemini com conta Pro real autenticada; a sessão de teste aberta estava deslogada, então nenhum pagamento ou acesso de conta foi simulado.
- O restante do P1 está corretamente separado entre bloqueios de integração/produção e decisões do proprietário: SMTP e testes de entrega (P1.10/P1.19), teste real das assinaturas Start/Pro/Consultoria, dados legais (P1.11), canal de suporte (P1.18), aprovação de fontes (P1.20) e operação/entrega editorial (e-book e atendimento da Consultoria). **P1.6 foi fechado com confirmação da exclusão em produção; P1.17 também está concluído.**

## O que existe de fato

### Atualização operacional — 20/09/2026

- `SUPABASE_DATABASE_URL` foi configurada no GitHub Actions com autorização do proprietário. O run manual #4 (`35486170058`) terminou com sucesso e armazenou artefato cifrado de 374 KB, com retenção de 30 dias. A correção dos binários PostgreSQL 17 está no commit `eb94f91`.
- Em 20/09, WSL 2, Docker Desktop e a CLI oficial do Supabase foram usados para restaurar o archive cifrado num stack local descartável. O manifesto, SHA-256 e AES-256-GCM foram validados; `pg_restore --exit-on-error` restaurou 11 tabelas públicas com RLS ativo, 527 linhas agregadas e 11 políticas num banco separado. Os 19 testes direcionados do backup e a suíte completa daquele ponto (202 testes) passaram. O stack, containers, volumes e pasta temporária foram removidos após a validação. A chave continua ignorada e não rastreada pelo Git; o proprietário confirmou que guardou uma cópia segura fora deste computador, concluindo **P0.11**.
- O simulador do Mercado Pago enviou `payment.updated` com `live_mode=false` e recebeu HTTP 200; essa evidência é apenas de conectividade. Em seguida, o script de replay foi executado contra uma compra aprovada existente, sem nova cobrança; o usuário confirmou a execução sem erro e os logs do Render registraram as duas entregas HTTP 200. Como o script só termina com sucesso quando ambas são verificadas e a segunda é idempotente, **P0.5 está concluído para a transação testada**.
- **P0.11 concluído:** backup diário cifrado, restore compatível validado e cópia externa da chave confirmada pelo proprietário. Referências abaixo à secret ausente, ao workflow ainda sem execução, à restauração compatível pendente ou à cópia externa pendente descrevem estados históricos anteriores à validação de 20/09/2026.
- O **P0 de segurança está 100% concluído no escopo acordado**: e-mail confirmado em Gmail/Outlook, replay assinado e idempotente do Mercado Pago, rate limiting distribuído fail-closed, checagem HIBP e backup cifrado com restore isolado foram validados. A MFA TOTP permanece voluntária por conta, protegida pelos testes e não é requisito para acessar contas sem autenticador. Um login real com TOTP pode ser validado depois, mas não bloqueia o P0.
- **P1.19 — e-mails de ciclo de vida:** a primeira entrega (`d385cd8`, Render Live) envia um lembrete opcional após 7 dias sem retorno, com agrupamento por frequência, outbox persistente, retries e retenção de 60 dias. A extensão preparada nesta rodada envia um resumo quando a pessoa registra manualmente uma candidatura como entrevista, apenas com opt-in explícito, usando o e-mail confirmado no Supabase e a outbox privada. Preferências antigas não são tratadas como consentimento. Notificações diárias/semanais são agrupadas; “imediatamente” está descrito como até 5 minutos. A opção de vagas expirando fica marcada “Em breve” e desligada porque não há data de expiração confiável. A configuração SMTP está salva; a entrega real aguarda confirmação de endereço e teste numa caixa confirmada. E-mail de boas-vindas duplicaria a confirmação do Supabase; avisos de outras mudanças de etapa não são úteis enquanto a própria ação é manual.
- **P1.20 — gate e inventário por fonte preparados:** `SOURCE_GOVERNANCE.md` registra as nove fontes prioritárias, Lever, Greenhouse, Adzuna, Jooble e uma decisão individual para os 30 domínios classificados; 0 têm autorização arquivada. `app/source_governance.py` exige aprovação, evidências, endpoint exato, direitos de coleta e exibição por padrão, escopos adicionais, limites e revisão a cada 180 dias. Os 14 testes de governança passaram; a coleta automática continua desativada até uma autorização específica/licença aplicável.
- **P1.17 — correções adicionais publicadas (`f9ca3ff` e `ececfdb`, Render Live):** `/candidaturas` mantém a lista quando métricas/lembretes falham e oferece retry independente; `/configuracoes` bloqueia salvamento até receber preferências válidas, trata 401/500/rede e oferece retry; `/seguranca` restaura controles em falhas de rede/timeout e anuncia o estado por ARIA. Naquela rodada, 308 testes da suíte passaram, mais 4 cenários simulados de configurações e 5 de segurança; Render confirmou `ececfdb` Live e health check HTTP 200. A revisão visual residual citada naquele registro foi concluída em `4f23938`.
- **Assinaturas mensais Mercado Pago:** Start (R$ 34,90), Pro (R$ 99,00) e Consultoria (R$ 197,00) usam preapproval mensal, webhook e cancelamento. A Consultoria dá direito a 1 sessão online de até 60 min por ciclo pago, com solicitação e agendamento pelo WhatsApp dedicado. Testes locais cobrem checkout, webhook, idempotência e acesso ao crédito; os ciclos ainda aguardam compra controlada em produção.
- **P1.9 concluído e publicado** no commit `d4102ff`: a faixa salarial identificada agora atravessa a fila e a promoção para vaga, junto da modalidade, regime e confiança já persistidos; testes direcionados passaram (20/20) e o deploy está Live.
- **P1.17 publicado no Render:** `e14086c` trouxe o tour de primeiro acesso com progresso por conta e estados assíncronos acessíveis; `414576f` acrescenta edição de experiências, competências, formação e idiomas, preservação das seções editadas e dos metadados nas reimportações, e correção para manter todas as linhas visíveis e salváveis. O Render confirmou `414576f` como **Live**. Nesta rodada, os testes unitários disponíveis passaram e o módulo `test_ats_registry_domains.py` também foi executado separadamente com o pytest empacotado (29/29).

### Aplicação e acesso

- API FastAPI com dashboard responsivo para celular e computador.
- Deploy público no Render, com health check em `/health`.
- Login, cadastro, logout, recuperação e alteração de senha pelo Supabase Auth.
- Cookies de sessão com `HttpOnly`, `SameSite=Lax` e `Secure` configurável por `COOKIE_SECURE`.
- Cookie de acesso e refresh renovados pelo middleware quando necessário.
- Formulário de cadastro com aceite obrigatório dos Termos de Uso e da Política de Privacidade.
- O cadastro registra no metadata do usuário as versões dos Termos/Privacidade e o instante UTC do aceite; o backend rejeita chamadas sem os dois aceites explícitos.
- Confirmação de e-mail é um gate explícito: sessão, login, callback de confirmação e rotas protegidas recusam contas pendentes; há página própria e reenvio limitado.
- O botão `Sair` existe no shell autenticado e na área de Segurança.

### Perfil, currículo e documentos

- Importação de currículo em PDF textual, DOC e DOCX.
- Extração de nome, resumo, experiências, competências, formação e dados de contato.
- Perfil profissional editável e preferências de cargo, localização, modalidade, palavras-chave e score.
- Estúdio independente em `/criar-documentos` para informar cargo, empresa, local, URL e detalhes da vaga.
- Geração de prévia de currículo adaptado e carta de apresentação personalizada.
- Prévia gratuita com conteúdo limitado; arquivos completos são liberados por plano ou compra avulsa.
- Geração de DOCX do currículo e da carta após autorização de exportação.
- Fluxo de importação mostra o estado concluído e permite substituir o currículo.
- `/curriculos` reúne o currículo principal importado e o histórico de currículos/cartas personalizados, com versões persistidas, downloads autenticados por proprietário, retenção e exclusão LGPD.

### Captação e análise de vagas

- Cadastro manual e captação por texto copiado.
- Captação por print, imagem e PDF com OCR local.
- Recuperação de informações de páginas públicas e dados `JobPosting` quando disponíveis.
- Integração Gmail com OAuth individual e escopo `gmail.readonly`.
- Monitor automático do Gmail e separação de alertas-resumo em vagas individuais.
- Integração Outlook/Microsoft Graph com escopo de leitura e monitor equivalente.
- Detecção de duplicidade e registro de mensagens já processadas.
- Parser de localização, modalidade e salário em vários formatos.
- Modalidades Presencial, Híbrido e Remoto já são reconhecidas; a prévia de intake agora também devolve confiança de modalidade, salário e regime.
- Análise de aderência com score, pontos fortes, lacunas, recomendação e justificativa.
- Motor de decisão com `AUTOMATICA`, `REVISAR` e `DESCARTAR`.
- Confiança de captura abaixo de 80%, localização/modalidade ausentes e pendências relevantes levam a revisão.
- Detecção de sinais de vaga suspeita, cobrança indevida e contratação PJ/MEI apresentada como emprego.

### Fila, vagas e candidaturas

- Fila persistente com aprovação, recusa, expiração e promoção para vaga/candidatura.
- Dashboard com filtros de decisão e status, busca por cargo/empresa e contadores.
- Paginação da fila com 5, 10 ou 25 itens por página.
- Estado vazio com ação direta para captar uma vaga.
- Cabeçalho reorganizado com CTA único `+ Captar vaga`, menu `Ações` e Preferências compactas.
- Banco de vagas com busca, origem, localização, modalidade, salário e detalhes completos.
- Detalhamento da vaga abre dentro do site e exibe análise, currículo, carta e próximo passo.
- Botão para abrir o anúncio original e iniciar candidatura no site da plataforma.
- O sistema registra a candidatura enviada quando o usuário conclui o envio na plataforma externa.
- A candidatura automática geral ainda não está habilitada.

### Pagamentos

- Checkout de exportação integrado exclusivamente com Mercado Pago; o fallback de URL externa legado foi removido e toda compra avulsa exige candidatura vinculada.
- Rotas, permissões e variáveis de InfinitePay foram removidas do fluxo ativo; o checkout e o webhook aceitos são exclusivamente do Mercado Pago.
- Webhooks consultam o status no provedor antes de liberar a exportação.
- Compras são associadas ao usuário e ao `order_nsu`.
- O webhook continua sendo a fonte de verdade e, ao confirmar o pagamento, grava uma tarefa durável que gera e arquiva os dois documentos no servidor, com tentativas automáticas, lease recuperável e idempotência entre instâncias. A geração não depende mais de o usuário retornar ao navegador; o estúdio mostra o andamento, permite recuperar o estado e oferece retry autenticado quando as tentativas automáticas se esgotam.
- `receipt_url` é persistida quando o provedor informa o endereço do recibo.
- O checkout grava o e-mail do pagador e o webhook confirmado tenta enviar um recibo transacional SMTP; o estado `SENT`, `FAILED` ou `SKIPPED` evita duplicidade e permite retry idempotente. O envio exige uma configuração SMTP completa; naquela inspeção, faltava `SMTP_PASSWORD`; a conferência atual a mostra salva, mas falta confirmar a entrega real.
- Currículo e carta são arquivados mesmo se o navegador fechar e a entrega por e-mail é tentada depois que a geração fica pronta; falha/ausência de SMTP não bloqueia a biblioteca nem a confirmação da compra.

### Segurança já publicada

- Middleware adiciona `Content-Security-Policy`, `Strict-Transport-Security` em HTTPS, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy` e `Permissions-Policy`.
- Rate limiting em memória por IP e identificador de conta para login, cadastro, recuperação de senha, reenvio de confirmação e alterações de credenciais.
- Limite retorna HTTP 429 com `Retry-After`.
- Dados de negócio filtrados por `owner_id` nas rotas principais.
- Testes de isolamento entre dois usuários cobrem listagem, consulta, atualização e downloads de vagas/candidaturas; o teste de cobertura garante que a migração RLS inclui todas as 17 tabelas do modelo.
- Refresh tokens de Gmail e Outlook cifrados com Fernet usando `TOKEN_ENCRYPTION_KEY`.
- Uploads têm limites de tamanho no backend: 5 MB para currículo e 10 MB para arquivos de vaga, com validação central de assinatura/magic bytes e decodificação de imagens.
- Conteúdo de vagas, e-mails e OCR passa por sanitização central antes de persistência ou análise; o frontend continua usando `textContent`/escape nas superfícies de exibição.
- Segredos são configurados por variáveis de ambiente e não devem ser colocados no Git.
- Assets estáticos usados pelas páginas autenticadas são servidos por rota pública com proteção contra traversal; isso permite carregar os controles de segurança sem sessão adicional.

### Legal e privacidade já existentes

- `/termos` com Termos de Uso básicos.
- `/privacidade` com Política de Privacidade básica.
- `/seguranca` com alteração de e-mail, alteração de senha, integrações e configuração de autenticador.
- O cadastro exige aceite dos Termos e da Política.

### Operação e serviços externos já configurados

- Render Web Service com `autoDeploy: true` e `healthCheckPath: /health` definido em `render.yaml`.
- Supabase PostgreSQL e Supabase Auth são os serviços de dados e autenticação da produção.
- Google Cloud OAuth/Gmail e Microsoft Graph/Outlook são integrações externas autorizadas pelo usuário.
- Mercado Pago é o único provedor externo de checkout no escopo ativo.
- UptimeRobot é usado como monitor externo de disponibilidade do serviço público/health check. O monitor, intervalo e contatos de alerta não são armazenados no Git; devem ser conferidos diretamente na conta UptimeRobot quando houver auditoria operacional.
- Nenhuma dessas configurações externas deve ser recriada como se estivesse ausente sem antes verificar o painel do respectivo provedor.

## O que está parcial ou ainda não existe

- O gate de confirmação de e-mail está implementado; `/termos` e `/privacidade` retornaram 200 sem sessão em produção com CSP/HSTS/nosniff ativos. A configuração do Supabase Auth foi conferida diretamente: `Confirm email` está ligado, o provedor Email está habilitado e o cadastro permanece permitido. O fluxo de reenvio tem regressão automatizada para normalização, URL pública, mensagem genérica e rate limit. Em 18/09/2026, o recebimento e o reenvio foram confirmados em Gmail e Outlook; **P0.4 está concluído**.
- InfinitePay está fora do escopo e não possui rota, variável ou critério de aceite ativo.
- O card de onboarding consulta `/profile` e `/preferences`: some quando os dois estão completos e vira um atalho de preferências quando o perfil já existe; mantém fallback estático se a consulta falhar.
- Logout existe no dashboard e agora também aparece como ação explícita no cabeçalho global das subpáginas autenticadas.
- Rate limiting combina o limite local com Redis REST compartilhado no Upstash Free. URL/token estão como secrets no Render e `RATE_LIMIT_DISTRIBUTED_REQUIRED=true` está alinhado no painel e no Blueprint. Após o deploy `5d736f2`, `/health` retornou 200 e uma tentativa sintética de login recebeu 401 do Auth, sem 503 do Redis. A prova simultânea entre várias instâncias fica para quando o serviço for escalado.
- CSP usa nonce para scripts e elementos `<style>`, sem `style-src-attr unsafe-inline`; os templates ativos e scripts de interação foram migrados para classes/CSS nonceados. HSTS/nosniff/frame-ancestors continuam confirmados em `/health`, `/termos` e `/privacidade`.
- IDOR/RLS está validado nas rotas atuais: testes locais e prova com duas contas reais cobrem leitura, atualização, exportação JSON e downloads cruzados, com 404 sem dados pessoais para a conta não proprietária. Repetir após adicionar novas rotas ou tabelas.
- O script `scripts/migrate_rls.py` cobre as 17 tabelas do modelo. Em 21/09, a migração idempotente foi aplicada no PostgreSQL de produção; uma consulta somente leitura confirmou as 17 tabelas presentes e com RLS habilitado, com políticas owner-scoped nas tabelas de usuário e sem acesso de usuário à outbox interna.
- A aplicação usa `SUPABASE_PUBLISHABLE_KEY`, não há `SERVICE_ROLE_KEY` no código, no `render.yaml` ou na lista de variáveis exibida no Render; o painel do Supabase separa as chaves e os valores permaneceram mascarados durante a auditoria; a conexão efetiva de produção foi verificada após o reinício com `Enforce SSL` ativo.
- A validação central de upload confere extensão, tamanho, magic bytes e estrutura/decodificação; o PDF falso foi rejeitado, o PDF válido reconhecido e a importação do currículo foi confirmada em produção. **P0.2 está concluído**; limpeza de artefatos segue em P1.8.
- Processamentos temporários são removidos no fluxo e documentos gerados ficam fora da raiz em diretório `0700`; falta ligar expurgo de rascunhos/objetos do Storage.
- A sanitização central de texto cobre captura, e-mail/OCR, confirmação e análise; superfícies HTML ativas são renderizadas com escaping e o CSP não aceita atributos de estilo inline.
- O avaliador da Gemini delimita pergunta, contexto e resposta como dados não confiáveis, sanitiza o conteúdo e instrui o modelo a ignorar comandos embutidos; testes hostis cobrem essa fronteira.
- A verificação contra senhas comprometidas usa a API gratuita da HIBP com k-anonimato (somente prefixo de 5 caracteres do SHA-1, nunca a senha ou o hash completo), está ligada por `PWNED_PASSWORD_CHECK` no Render e agora falha fechado se o serviço responder com erro ou dados inválidos. O fluxo é aplicado no cadastro e na alteração de senha; a proteção nativa do Supabase fica como defesa adicional opcional, sem exigir upgrade para fechar o gate do produto.
- A rota do Mercado Pago é pública, consulta o provedor e faz transição idempotente para `PAID`; o endpoint e o evento Pagamentos estão configurados, os segredos permanecem no Render e o retorno de uma compra já aprovada foi validado em produção, gerando os documentos. O replay assinado da mesma compra foi repetido duas vezes sem nova cobrança; **P0.5 concluído para a transação testada**.
- As rotas de download verificam o `owner_id` da candidatura e exigem uma compra `PAID` vinculada ao `application_id` exato; o checkout recebe a candidatura e os testes locais e reais com duas contas confirmaram bloqueio cruzado.
- O normalizador de `DATABASE_URL` converte PostgreSQL para `psycopg` e força `sslmode=require`; `Enforce SSL` e a conexão pós-reinício foram confirmados no Supabase. TLS está concluído para o ambiente atual.
- Handlers globais já padronizam erros públicos e mantêm detalhes nos logs; falta revisar endpoints operacionais legados.
- OCR, parsing, confirmação e busca externa já saem do loop HTTP e têm timeout total de 30 segundos; falta separar monitores/IA em worker próprio e impor limites distribuídos de concorrência/CPU.
- O runbook `DISASTER_RECOVERY.md` documenta backup, restauração isolada e revogação/rotação emergencial. O GitHub Actions mantém artifact cifrado por 30 dias. Em 20/09/2026, WSL 2, Docker Desktop e a CLI oficial do Supabase restauraram o archive completo num banco descartável separado; foram verificadas 11 tabelas públicas, 527 linhas agregadas e 11 políticas RLS, e o stack temporário foi removido. A chave privada é ignorada e não rastreada pelo Git, e o proprietário confirmou uma cópia segura externa. **P0.11 concluído.**
- A prévia de intake classifica CLT, PJ, MEI, estágio, temporário e freelance e informa confiança para modalidade, salário e regime; esses campos agora persistem na fila, na vaga promovida, no monitor Gmail e nas respostas de vagas/exportação. A faixa salarial oferecida também é guardada em `salary_min/salary_max`, separada das preferências de pretensão do candidato.
- **P1.10 publicado no Render em `7bc618a`:** o webhook de pagamento confirmado enfileira a geração no servidor; a fila durável retoma tentativas interrompidas, persiste versões privadas em PostgreSQL e habilita downloads owner-scoped no histórico de `/curriculos`, sem depender do navegador. O retry manual é autenticado e idempotente; falha de e-mail não desfaz a geração. As variáveis públicas do relay estão salvas no Render; as variáveis SMTP já aparecem salvas; falta confirmação do endereço e entrega real de recibo/documentos.
- Nesta rodada P1.3/P1.6: **268 testes unittest passaram**; os 49 testes direcionados de intake, qualidade, decisão e monitor Gmail e os 7 testes direcionados de exclusão/portabilidade passaram. O módulo `test_ats_registry_domains.py` requer `pytest`, ausente no runtime empacotado, e não foi alterado nesta rodada.
- A retenção local de documentos e dados brutos roda no startup e periodicamente; o subescopo de expurgo para a arquitetura atual está concluído em **P1.8**. Expurgo de objetos no Storage só entra se um bucket e artefatos persistidos forem adotados.
- A exportação/portabilidade JSON owner-scoped está disponível na área de Segurança. **P1.6 concluído:** a exclusão inclui assinaturas, relações/documentos e demais registros do proprietário; confirma o commit local antes de remover a identidade Supabase, aceita 404 como retry idempotente e interrompe a exclusão se um arquivo legado privado não puder ser removido. Os testes isolados cobrem proprietário A/B e falhas simuladas; o proprietário confirmou a exclusão definitiva de uma conta de teste em produção. Não há bucket de Storage ativo.
- Consentimento de Termos/Privacidade agora é versionado e recebe data UTC no cadastro; uma trilha imutável administrativa continua opcional para uma etapa futura.
- O monitor Gmail/Outlook ainda roda junto do processo web; falta worker distribuído independente.
- O monitor UptimeRobot não é controlado pelo código; alterações de intervalo, URL ou alertas precisam ser feitas no painel do UptimeRobot.
- A busca de páginas públicas já bloqueia hosts e IPs não globais, valida cada redirecionamento e limita o corpo recebido; a proteção contra SSRF precisa permanecer coberta por testes de regressão.
- O OCR usa arquivos temporários e os remove em `finally`; documentos gerados usam diretório privado e retenção configurável. Expurgo de Storage/rascunhos ainda falta.
- Não existe painel administrativo multiusuário.
- Não existe aprendizado baseado em entrevistas, aprovações e reprovações.
- O ciclo de resultado agora registra canal, retorno externo e identificadores de versão do currículo/carta no evento de candidatura enviada; falta acumular uma amostra real para atribuição estatisticamente confiável.
- O simulador agora pode carregar um roteiro a partir dos gaps da candidatura selecionada e envia esse contexto à avaliação Gemini do plano Pro; a integração desta rodada ainda aguarda publicação e confirmação em produção.
- Não há acompanhamento automático da zona morta após a candidatura, com prazo, lembrete e sugestão de follow-up apropriado ao canal.
- A proteção contra golpes agora preserva a faixa, score e evidências ao promover a vaga para candidatura; vagas duvidosas exigem revisão explícita antes de abrir o anúncio. Falta ampliar a cobertura por origem e validar o fluxo em produção.
- As heurísticas de RH brasileiro ainda não estão formalizadas como regras versionadas e explicáveis do motor de análise.
- Não existe visão Kanban, extensão de navegador ou exportação operacional para CSV/Excel.

## Divisor de águas incorporado ao planejamento

As ideias de diferenciação foram incluídas como produto de resultado e hábito, sem antecipar a extensão antes de fechar segurança, métricas e fluxo de candidatura:

- **Copiloto de entrevistas baseado em gaps:** usar os requisitos ausentes e os pontos fracos da análise para gerar perguntas prováveis, respostas orientadas por evidências do currículo e um plano de estudo por vaga.
- **Acompanhamento da zona morta:** iniciar o relógio quando a candidatura for registrada, sugerir lembrete e follow-up conforme o canal e permitir registrar resposta, entrevista ou encerramento.
- **Prova social por conversão:** mostrar a sequência vagas analisadas → candidaturas enviadas → entrevistas qualificadas, segmentada por origem e versão de currículo/carta, sem prometer aumento antes de haver amostra suficiente.
- **Extensão de navegador 1-click:** permanece como aposta de escala no P2; deverá usar permissões mínimas, consentimento explícito e captação autorizada em cada site.

## Direção de produto incorporada ao planejamento

As orientações de produto foram lidas junto com o histórico técnico e foram incorporadas sem deslocar o P0 de segurança, privacidade e pagamentos. A ordem adotada é:

- **Resultado medido antes de escala:** registrar fonte, versão do currículo/carta, decisão, candidatura enviada, retorno, entrevista qualificada e desfecho. A métrica principal continua sendo entrevistas qualificadas por 100 candidaturas.
- **Proteção contra golpe como proposta central:** ampliar os sinais de vaga falsa, cobrança indevida, PJ disfarçado e inconsistências do anúncio para uma decisão de risco visível antes de abrir ou enviar a candidatura.
- **Conhecimento de RH como regra do produto:** transformar critérios de triagem, ATS, pretensão salarial, regime e sinais de vaga fantasma em heurísticas versionadas, justificadas e testáveis, complementando a IA.
- **Prova antes de promessa:** qualquer alegação de aumento de conversão deverá vir de dados observados no produto; não será apresentada como promessa antes de haver amostra e atribuição suficientes.

## Visão de evolução do produto

| O que o site é hoje | O que o site será após a Prioridade 0 | O que faz o site dominar o mercado |
| --- | --- | --- |
| Painel operacional com boa base visual e segurança em consolidação. | SaaS estável e seguro, com P0 concluído, checkout funcional e UX redonda. | Copiloto completo de carreira: análise, documentos, preparação para entrevistas e extensão de navegador. |

## Auditoria das sugestões recebidas

As sugestões abaixo foram comparadas com o código, os testes, os painéis já verificados e a fila desta página. O destino indica a forma necessária para realmente encerrar o item.

| Sugestão | Veredito | Destino correto e complemento necessário |
| --- | --- | --- |
| Reorganizar ações do cabeçalho, CTA único, menu de documentos e paginação | Já feito | Sem nova tarefa; manter regressão visual e paginação funcionando |
| Badges, estados vazios, busca e contadores | Parcialmente feito | Badges, estados vazios e busca cargo/empresa existem; busca por tecnologia fica junto das heurísticas em **P1.3** se houver necessidade real |
| Kanban, donut de score e exportação CSV/Excel operacional | Faz sentido, mas não é gate | **P2**; exportação de dados pessoais para LGPD é separada e permanece em **P1.6** |
| Onboarding sincronizado e opção de dispensar | Concluído | **P1.13 entregue:** o tour acompanha o perfil e as preferências reais, salva o progresso por conta e só é dispensado por ação explícita; não reabrir como pendência |
| Extensão Chrome/LinkedIn/Gupy | Faz sentido como escala | **P2**, com permissões mínimas, consentimento e limites de cada plataforma |
| Foco, `aria-modal` e retorno de foco dos modais | Validado em produção | Modais de captação, preferências, segurança e drawer de candidatura abriram com foco inicial, mantiveram o Tab dentro da superfície e devolveram o foco ao disparador |
| Verificação de e-mail, headers e rate limiting | Validado no ambiente atual | **P0.4**, **P0.7** e **P0.9** estão concluídos; manter teste entre instâncias quando houver escala horizontal |
| IDOR e RLS obrigatório | Concluído para as rotas atuais | Migração RLS, testes locais e prova real com duas contas cobrem listagem, consulta, atualização, exportação JSON e downloads de currículo/carta; a conta B recebeu 404 ao tentar usar a candidatura da conta A |
| `SERVICE_ROLE_KEY` fora do cliente e menor privilégio | Revisão de código feita; painel do Supabase separa chave publicável e chave secreta e mantém os valores mascarados; Enforce SSL foi ativado no banco; função auxiliar `public.rls_auto_enable()` não pode mais ser executada por `PUBLIC`, `anon` ou `authenticated` | **P0.6** confirmado para chaves, TLS e privilégio da função; não criar nem expor chave mestra |
| Magic bytes, MIME real, diretório privado e parsing isolado | Validação real aprovada para intake; fronteira de download reforçada | Em produção, um PDF falso sem assinatura `%PDF-` foi rejeitado sem criar vaga, um PDF válido foi reconhecido no preview e o currículo importado apareceu como pronto na tela de Currículos. As rotas de download rejeitam caminhos fora do diretório privado; a limpeza periódica de artefatos permanece em **P1.8** |
| Gmail `readonly` e tokens criptografados | Feito no código | Manter auditoria de escopos e revogação; não criar escopos maiores |
| Sanitização/XSS e prompt injection | Concluído para os fluxos atuais | Sanitização central, fronteira de prompts, CSP sem `style-src-attr unsafe-inline` e regressões de Gmail/Outlook/arquivos; novas fontes ou prompts exigem cobertura própria |
| Assinatura, replay e idempotência de webhook | Código reforçado e agora fail-closed | Mercado Pago exige HMAC com janela de 5 minutos, consulta server-to-server, moeda BRL, valor exato, `order_nsu`/`payment_id` e transição idempotente; duas entregas reais assinadas da mesma compra aprovada retornaram HTTP 200, com replay idempotente confirmado pelo gate do script. **P0.5 concluído para a transação testada** |
| Mensagens de erro genéricas | Feito no código | Handlers, fila e callbacks OAuth não expõem stack trace, detalhes de banco ou respostas brutas de provedores; manter revisão ao adicionar endpoints |
| OCR/IA assíncronos e timeout de 30 segundos | Proteção principal feita | Worker separado é escala operacional e fica em **P2**; não deve bloquear o primeiro ciclo pago enquanto os timeouts forem aplicados |
| Backup diário, restauração e revogação | Secret configurada; workflow diário e artifact cifrado por 30 dias; restore do archive em Supabase local descartável confirmou 11 tabelas, 527 linhas agregadas e 11 políticas RLS; o proprietário confirmou a cópia segura externa da chave | **P0.11 concluído** |
| Cloudflare, DNS e proteção de borda | Domínio próprio `candidaturacerta.com.br` associado ao Render; DNS do site pronto na zona, aguardando propagação do Registro.br; e-mail de entrada ainda não configurado | **P2.7**; concluir propagação, verificar certificado/redirect e configurar o endereço base/callbacks; preservar os registros de e-mail existentes |
| Termos, privacidade, consentimento, portabilidade e exclusão | Parcialmente feito | Páginas e aceite existem; consentimento versionado é **P1.7**, exportação/exclusão no mesmo fluxo é **P1.6**, textos legais em **P1.11** |
| Acesso anônimo às páginas legais | Corrigido: `/termos` e `/privacidade` (com barra final) foram adicionadas à lista pública do middleware e testadas sem sessão | P0 concluído e validado em produção |
| Retenção de 30/60 dias | Necessário, mas não é gate de pagamento | Diretório privado e limpeza local já existem; expurgo de Storage, prints e rascunhos fica em **P1.8**, com prazo configurável e registro da exclusão |
| Comprovante por e-mail | Envio após `PAID` e estado idempotente implementados; falta SMTP de produção e teste real | **P1.10 concluído no código**; concluir após configurar/testar SMTP |
| Currículo e carta enviados por e-mail após a compra | Os dois DOCX são enviados como anexos neutros ao e-mail confirmado, somente após geração e persistência; erros ficam visíveis com retry | **P1.10 concluído no código**; envio real depende de SMTP configurado |
| Aba de documentos e histórico de downloads | `/curriculos` lista versões por candidatura, com download autenticado, retenção e exclusão LGPD; documentos duráveis em PostgreSQL | **P1.10 concluído no código** |
| CLT/PJ/MEI, modalidade, salário e pretensão | Regime, modalidade, texto/faixa salarial e confiança são persistidos da captura manual/Gmail até a fila e a vaga promovida; pretensão do candidato permanece em preferências separadas | **P1.9 concluído** para o intake atual; expansão por fonte permanece em P1.3/P2.2 |
| Métrica de entrevistas por 100 candidaturas | Divisor de águas | **P1.1 entregue no código**: registra canal, retorno externo e versão do documento; o painel só aponta melhor canal/versão após cinco envios na mesma amostra |
| Proteção contra golpes | Deve ser destaque de produto | **P1.2 publicado no Render (`720565a`)**: score e sinais são recalculados também para registros antigos, conteúdo alterado invalida revisão anterior, sinais de fraude bloqueiam promoção/envio e a revisão de risco fica auditável. A expansão separada das heurísticas de intake está em P1.3; não duplica o motor de risco |
| Heurísticas de RH brasileiro | Proveniência pelo domínio declarado do remetente/link individual, split conservador de alertas, campos ambíguos mantidos desconhecidos, decisão versionada `br-rh-2` e reason codes auditáveis; a correção `0d1523b` impede confundir valor isolado de benefício com salário | **P1.3 publicado**, 268 testes unittest passam; fixtures sintéticas, ampliar validação com mensagens reais anonimizadas quando disponíveis |
| Copiloto de entrevistas e follow-up da zona morta | Faz sentido após medir eventos | **P1.4/P1.5**, dependem do registro correto de candidatura, retorno e entrevista |
| Candidatura automática geral | Não deve ser liberada agora | Continua desativada até fechar P0 e validar termos/fluxos de cada plataforma; abrir anúncio e iniciar candidatura com confirmação do usuário é o comportamento seguro atual |
| InfinitePay | Fora do escopo ativo | Não criar tarefa nem critério de aceite para esse provedor |
| UptimeRobot | Já existe | Não recriar; apenas conferir painel, intervalo e alertas durante auditoria operacional |
| Senhas comprometidas | Alternativa gratuita HIBP com k-anonimato e fail-closed publicada no commit `1aa7da4`, confirmado Live no Render; a proteção nativa Supabase Pro+ é opcional | **P0.10** não exige upgrade; manter teste de regressão |

### Auditoria visual e de intake — 17/09/2026

As telas anexadas foram tratadas como evidência de comportamento, não como instruções isoladas. O código e os testes foram comparados antes de abrir novas tarefas:

| Achado | Situação após a verificação | Prioridade correta |
| --- | --- | --- |
| CSS de Outlook/Gmail vazando no título | Corrigido no parser HTML do Gmail: `style`, `script`, `noscript` e `template` são ignorados antes da sanitização; teste de regressão adicionado | P0.2 concluído no código; manter validação operacional de uploads/intake |
| URL pura usada como título | Corrigido no intake: slug de URL vira título legível (`Analista Administrativo`) | P0.2 concluído no código |
| Razão social com prefixo numérico de CNPJ | Corrigido no intake removendo o prefixo sem apagar o sufixo societário | P0.2 concluído no código |
| Alertas “27 vagas abertas...” salvos como uma vaga | Split conservador por links de vagas individuais ou cartões com título de cargo, proveniência por domínio/link e ignorar resumos que não identificam vagas; nenhum dado ambíguo é inventado | **P1.3 publicado e Live**; validar formatos exatos por provedor com mensagens reais anonimizadas quando disponíveis |
| Cards, badges, paginação, busca e estado vazio da fila | Já implementados; a incoerência textual entre “Aguardando revisão” e “Capturar” é nomenclatura de produto a revisar junto das heurísticas | P1.3 se houver mudança de decisão; sem novo P0 |
| `/vagas` exibe erro sem ação | Corrigido com botão local `Tentar novamente`, sem exigir F5; backend já retorna mensagem pública estável | P1.15 concluído no código e a rota foi reaberta em produção sem erro, exibindo estado vazio e contadores `0` |
| Modal abre com textos repetidos “Carregando...” | Ainda não há skeleton screen; trocar por blocos de carregamento é melhoria de percepção, sem impacto de segurança | P1.15 |
| Cabeçalho escuro duplicado no dashboard e subpáginas | Dashboard mantém seu cabeçalho próprio; subpáginas, incluindo `/seguranca`, mantêm somente a navegação global e o breadcrumb | **P1.15 concluído e validado em produção** |
| Botões Aprovar/Recusar genéricos e subtítulo de alerta repetido | Decisão e status são dados distintos; renomear ações e reduzir o subtítulo exige revisar copy e transições | P1.3, junto do modelo de decisão; não implementar só por aparência |
| Botão Limpar próximo da ação principal | Corrigido: virou `Limpar formulário`, com aparência neutra, confirmação quando há rascunho/prévia e retorno de foco ao primeiro campo | **P1.15 concluído e validado em produção** |
| Visualização da senha no login/cadastro/alteração | Controles mostrar/ocultar já existem; a tela de Segurança também teve o texto auxiliar do MFA corrigido para ficar em linha própria | P1.15 |
| Placeholders de salário ausentes | Adicionados exemplos `Ex.: 8.000` e `Ex.: 12.000` nas configurações | P1.9 concluído no código |
| R$ 9,90 avulso, FAQ e prova social na landing | A linha do avulso pode ser adicionada com o preço real; FAQ é copy útil; números de prova social só entram quando vierem de métricas observadas | P1.16; métricas de conversão permanecem P1.1 |
| Queda da rota `/vagas` após o carregamento | Corrigido no `jobs-enhance.js`: o `MutationObserver` não reordena a lista em ciclo contínuo; a página foi revalidada em produção com lista, filtros, detalhes e análise carregados | P0.8 concluído para esta causa; manter teste de regressão operacional |

### Auditoria de UX/UI e Customer Success — 18/09/2026

As novas sugestões foram comparadas com o que já está publicado. Os percentuais abaixo medem implementação, teste e validação compatíveis com o escopo do item; não representam uma avaliação subjetiva de gosto visual.

| Frente sugerida | Estado atual | Conclusão estimada | Destino correto |
| --- | --- | ---: | --- |
| Layout, estrutura responsiva, dashboards, perfil e funil | Dashboards essenciais, busca, paginação, estados vazios, perfil e funil de candidaturas existem; métricas de conversão estão instrumentadas em **P1.1** | **95%** | Sem nova tarefa; manter regressões |
| Refinamento de componentes, microinterações e consistência visual | Ações do cabeçalho, botões neutros, skeletons, retry, feedback de upload, launcher e layouts autenticados foram tratados e revisados em produção | **100%** | **Concluído em P1.17 (`4f23938`, Render Live)** |
| Favicon e identidade visual na aba do navegador | Favicon SVG exclusivo da Candidatura Certa publicado e injetado no shell público, páginas legais e páginas autenticadas; o arquivo e o shell foram confirmados no Render | **100%** | **Concluído em P1.17** |
| Hierarquia dos dados complexos do OCR durante a edição | `/perfil` permite editar experiências, competências, formação e idiomas, e as seções alteradas são preservadas nas reimportações sem apagar os demais metadados | **100%** | **Concluído em P1.17 (`414576f`, Render Live)** |
| Onboarding guiado em três passos | Tour existente agora é isolado por ID autenticado, retoma do passo salvo e só conclui por ação explícita de pular/fechar; links externos pausam | **100%** | **Concluído em P1.17** |
| Feedback assíncrono de OCR e validação de arquivos | Mensagens específicas, região acessível, skeleton do drawer/Banco de vagas/entrevistas/lookup do estúdio, retry e estados vazios/erro; movimento reduzido respeitado | **100%** | **Concluído em P1.17** |
| Erros de validação orientados à ação | `/curriculos`, entrevistas, lookup do estúdio e `/candidaturas` mantêm retry; falhas auxiliares de métricas/lembretes não ocultam mais a lista | **100% código nesta tela** | **P1.17** concluído (`4f23938`, Render Live) |
| Widget de suporte ou canal direto | Widget acessível carregado na landing e no shell autenticado, com resposta server-side limitada a conteúdo revisado e fallback para suporte humano; teste em produção confirmado | **100% do widget** | **P1.18**; entrega e recebimento de e-mails seguem no item separado P1.10/P1.19 |
| Central de ajuda e FAQ | FAQ da landing em **P1.16**; a central pública `/ajuda` cobre formatos, privacidade, pagamentos avulsos e recorrentes, cancelamento, entrega dos documentos e simulação com IA/local; o chat agora aponta diretamente para a central | **95%** | **P1.18** |
| E-mails de Customer Success | Lembrete após 7 dias e resumo opcional ao registrar entrevista implementados com opt-in, outbox idempotente, retentativas e exclusão/portabilidade LGPD; Brevo HTTPS é priorizada com SMTP como fallback; os fluxos mantêm opt-in explícito, destinatário confirmado, outbox idempotente, retries e exclusão após 60 dias. A entrega real e a qualidade das datas de origem ainda precisam ser validadas. Boas-vindas não será duplicada porque o Supabase já confirma a conta | **90% (estimativa)** | **P1.19**, sem duplicar o escopo de entrega paga |
| Entrega paga e histórico de documentos | Webhook enfileira geração no servidor; estado, lease, tentativas e retry são duráveis; os DOCX ficam em biblioteca owner-scoped, com download, retenção e exclusão. `7bc618a` está Live no Render; todas as variáveis SMTP aparecem salvas, mas a entrega real aguarda confirmação do e-mail da conta e reenvio para comprovação | **92%** | **P1.10 — confirmar o endereço e validar entrega** |

Essas sugestões não alteram a ordem do P0: nenhuma delas substitui os gates de isolamento, uploads, MFA, confirmação de e-mail, Mercado Pago, senhas comprometidas e backup. O suporte e os e-mails entram depois desses gates porque dependem de uma operação pagante estável.

### Auditoria de job scraping e ingestão de fontes — 18/09/2026

As sugestões de raspagem foram comparadas com a implementação atual e classificadas por dependência, risco legal e necessidade operacional. A conclusão não autoriza scraping de plataformas protegidas nem uso imediato de proxies: primeiro entram fontes licenciadas ou páginas públicas permitidas, com limites claros e possibilidade de desligar cada conector.

| Proposta | Estado atual | Conclusão e prioridade |
| --- | --- | --- |
| Captura por Gmail/Outlook e entrada manual por texto/arquivo | Já existe; o monitor identifica fontes, separa alertas agrupados, sanitiza o conteúdo e envia o resultado para a fila. A captação manual pode buscar uma página HTML pública com SSRF, redirecionamento, tamanho e timeout limitados | **Coberto em P0/P1**; manter regressões e ampliar amostras por provedor em **P1.3/P1.8** |
| Parser estruturado de modalidade, CLT/PJ/MEI, salário e senioridade | Parser e persistência cobrem modalidade, regime, salário bruto/faixa numérica e confiança por campo; salário oferecido segue separado das preferências. A fila agora preserva o texto salarial até a vaga promovida | **P1.9 concluído** para captura manual/Gmail; ampliar regras por provedor fica em P1.3 e novos conectores em P2.2 |
| Deduplicação de vagas | Parcialmente existe: `QueueItem` usa hash de URL limpa ou cargo/empresa/local, com contagem de reaparições; o intake também cria `external_id` determinístico | **P2 — completar identidade canônica** com `external_id` da fonte, URL canônica e fingerprint de conteúdo; fuzzy matching só como fallback revisável |
| APIs oficiais ou agregadores (por exemplo Jooble/Adzuna) | Não há adaptadores nem contrato ativo mapeado | **P2.1 novo**; comparar licença, custo, cobertura Brasil, limites e atribuição antes de desenvolver. Só fontes autorizadas entram |
| Conectores para ATS públicos (Gupy e portais de carreira) | Os links são reconhecidos e podem ser lidos quando fornecidos pelo usuário, mas não existe coleta agendada por ATS | **P2.2 novo**; piloto de duas ou três fontes públicas estáveis, cada uma com parser e chave de desligamento próprios |
| Fetcher → parser → deduplicador → enriquecedor | As peças existem de forma síncrona para intake e Gmail; não há pipeline de descoberta contínua | **P2.3**; consolidar uma interface de ingestão para reuso das peças atuais e incluir senioridade, modalidade, regime, salário e proveniência |
| Agendamento e workers assíncronos | OCR/IA e monitores ainda compartilham o processo web; workers separados e limites distribuídos já estão no P2 | **P2.4**; incluir scheduler, filas, concorrência por fonte, backoff e timeout. Não é requisito para o primeiro ciclo pago |
| Saúde dos conectores e alerta abaixo de 80% | UptimeRobot cobre a saúde do serviço, mas não mede sucesso por fonte/conector | **P2.5 novo**; registrar tentativas, sucesso, bloqueios, latência e parsing inválido, com alerta e pausa automática quando a qualidade cair |
| Anti-bot, browser stealth e proxies rotativos | Não existe atualmente | **P2.6 condicional**; manter na fila como capacidade opcional para fontes autorizadas, depois de licença/Termos/robots e necessidade comprovada. Usar limites, identidade transparente, sem contornar CAPTCHA ou bloqueios deliberados, com desligamento por fonte |
| Rate limit e proteção da origem | Rate limiting da aplicação está no P0; falta orçamento específico por fonte para uma coleta futura | **P2.3/P2.4**; aplicar intervalos, User-Agent identificável, backoff e teto por domínio antes de qualquer coleta agendada |
| Legalidade, robots/Termos, atribuição e retenção do dado bruto | `SOURCE_GOVERNANCE.md` e `app/source_governance.py` documentam e exigem um registro estruturado; o registro global permanece vazio, sem autorização específica de fonte | **P1.20 40%**; aprovar um quadro/fonte e seus usos comerciais antes de P2.1/P2.2, com proveniência e TTL definidos |

O desenho aprovado para a fila é, portanto: **P1.20** (legalidade e proveniência) → **P2.1** (APIs autorizadas) → **P2.2** (piloto ATS público) → **P2.3/P2.4** (pipeline, limites, scheduler e workers) → **P2.5** (observabilidade por fonte) → **P2.6** (proxies/stealth condicionados) → expansão gradual. Essa capacidade permanece planejada, mas só será ativada para fontes autorizadas e após os critérios legais, operacionais e de desligamento serem atendidos.

### Auditoria da proposta de DDL e worker de ingestão — 18/09/2026

Esta proposta foi tratada como desenho técnico para a fila, não como autorização para criar a tabela ou iniciar coletas. O modelo atual usa `jobs` para oportunidades do usuário e `queue_items` para a fila de decisão; ambos já têm proprietário, campos estruturados, sanitização e RLS. A proposta de `job_listings` representa um catálogo externo compartilhado e só deve ser adotada se essa distinção for necessária.

| Requisito técnico | O que já existe | Classificação e forma correta de completar |
| --- | --- | --- |
| Tabela `job_listings` com título, empresa, local, modalidade, regime, salário, URL, descrição e status | Não existe uma tabela separada; os campos equivalentes já estão em `jobs`/`queue_items`, com `contract_type`, `modality`, limites salariais, URL e descrição | **P2.3**; primeiro decidir se haverá catálogo compartilhado. Se a ingestão continuar por usuário, reutilizar os modelos existentes. Se houver catálogo global, criar uma tabela própria com `visibility`, `source_url/canonical_url`, proveniência, `first_seen_at/last_seen_at` e retenção, sem duplicar dados por acidente |
| Índice único `(source_name, external_id)` | `Job.external_id` é único e a fila usa `dedup_hash` por proprietário; não há chave composta por fonte e identificador externo | **P2.3 novo**; o catálogo global deve usar essa chave composta (ou constraint equivalente), normalização de fonte e tratamento explícito de identificador ausente |
| Valores controlados para modalidade, `contract_type` e `status` | O parser normaliza os valores brasileiros conhecidos e preserva rótulos explícitos menos comuns como texto; a fila registra decisão e status sem confundi-los com o estado da vaga | A restrição de domínio para conectores novos fica em **P2.3**; não forçar um enum agora que descartaria regimes/modalidades válidos |
| RLS com leitura pública/autenticada e escrita só por serviço/admin | RLS está ativo nas 11 tabelas atuais, com políticas owner-scoped; não existe política para um catálogo público | **P0.1/P0.6 como gates de aceite da nova tabela, dentro da entrega P2.3**; preferir leitura autenticada ou uma view pública mínima. Escrita deve ocorrer apenas no backend/worker com segredo server-side; nunca expor chave de serviço no cliente |
| Worker que busca ATS/API pública | O `job_source_fetcher` lê JSON-LD de uma URL pública fornecida pelo usuário; Gmail/Outlook e intake funcionam no processo web | **P2.2/P2.4**; criar adaptadores por fonte e worker agendado somente após P1.20 e aprovação da fonte |
| Sanitização e extração de palavras-chave | `sanitize_untrusted_text`, parser de título/empresa e classificadores de modalidade, regime e salário já existem; casos de alerta e HTML têm regressão | Cobertura por origem permanece em **P1.3**; futuros workers devem reutilizar o parser central em P2.4 |
| Upsert por `(source_name, external_id)` | `queue_service.enqueue` faz deduplicação por URL/hash e incrementa reaparições, mas não faz upsert de um catálogo por chave externa; o `external_id` de jobs promovidos é gerado de outra forma | **P2.3 novo**; implementar upsert transacional na constraint composta, atualizar `updated_at/last_seen_at`, marcar expiração e preservar histórico de origem |
| DDL/migração pronta para aplicação | Não há migração `job_listings` nem contrato de fonte ativo | **Não implementar agora**; elaborar a migração somente junto do primeiro conector aprovado, incluindo índices, constraints, RLS, teste IDOR e estratégia de rollback |

Decisão registrada: não criar `job_listings` apenas para satisfazer o formato sugerido. A ordem é **P1.20** (legalidade/proveniência) → **P2.1/P2.2** (fonte autorizada e piloto) → **P2.3** (modelo, constraint composta, upsert, parser reutilizado e deduplicação) → **P2.4** (worker/scheduler) → **P2.5** (saúde por fonte). Ao introduzir a tabela, a validação de RLS/IDOR e a revisão de segredos voltam a ser obrigatórias antes de publicar.


## Próximas prioridades

### P0 — antes de aceitar usuários pagantes

1. **IDOR/RLS real — concluído para as rotas atuais:** duas sessões reais foram separadas no navegador conectado; a conta B exibiu zero vagas/candidaturas, a abertura direta de `/dashboard?application_id=15` não revelou a candidatura da conta A e as rotas de exportação/download cruzadas retornaram 404 sem conteúdo pessoal. O download exige compra `PAID` vinculada à candidatura exata. Manter a regressão ao criar novas rotas.
2. **Uploads e arquivos — concluído para o gate P0.2:** PDF falso rejeitado, PDF válido reconhecido no preview, currículo importado com sucesso em produção e rotas de download confinadas ao diretório privado. O feedback global preserva respostas HTTP de erro para exibir a mensagem segura e específica do validador. A limpeza periódica de artefatos segue classificada em **P1.8**.
3. **MFA opcional por conta — correção ponta a ponta publicada (`03c0adb`, Render Live):** quem tem TOTP confirmado precisa de `aal2` em toda rota protegida; sessões `aal1` antigas, renovadas ou obtidas no callback de confirmação são bloqueadas e voltam ao login com desafio, enquanto contas sem fator continuam sem MFA obrigatório. A verificação de enrollment preserva a sessão elevada devolvida pelo Supabase. Cookies são limpos com `HttpOnly`, `Secure` e `SameSite=Lax`; desafios expirados ou respostas sem `aal2` não criam sessão. A tela deixa explícito que SMS/códigos de recuperação não existem e orienta a cadastrar o mesmo segredo TOTP em dois dispositivos antes da ativação. Os testes do fluxo opcional passaram e `/health` retornou 200 com banco conectado após o deploy. **P0.3 concluído no escopo acordado**; um login manual com TOTP é validação adicional, e não há recuperação automática se os dois dispositivos autenticadores forem perdidos.
4. **E-mail confirmado — concluído:** o Supabase mostra `Confirm email` ligado e Email habilitado; login/signup bloqueiam sessão não confirmada, o reenvio usa a URL pública e aplica resposta genérica/rate limit. O recebimento e o reenvio foram confirmados operacionalmente em Gmail e Outlook em 18/09/2026.
5. **Webhook Mercado Pago — código, configuração e replay publicados:** endpoint de produção, evento Pagamentos, `MERCADOPAGO_ACCESS_TOKEN` e `MERCADOPAGO_WEBHOOK_SECRET` estão ativos; o código rejeita valor divergente e moeda diferente de BRL, exige HMAC/replay/idempotência, retorna 503 se o Access Token faltar e 401 para assinatura inválida. O checkout reserva a aba no clique e mostra erros no próprio bloco; o Mercado Pago exibe os meios habilitados na conta, incluindo Pix quando disponível. O retorno da compra confirmada gerou os dois documentos e duas entregas assinadas da mesma compra foram aceitas, com idempotência confirmada pelo script. **P0.5 concluído para a transação testada.**
6. **Segredos, menor privilégio e TLS — concluído para o ambiente atual:** painel Supabase/Render sem chave mestra exposta, scanner do código versionado sem padrões de segredo, `Enforce SSL` ativo e aplicação forçando `sslmode=require`. Repetir a auditoria somente quando novas integrações forem adicionadas.
7. **CSP e superfícies de renderização — concluído:** scripts e elementos `<style>` usam nonce por resposta, todos os templates ativos foram migrados de atributos `style` para classes, a exceção `style-src-attr unsafe-inline` foi removida e o dashboard/rotas legais retornaram CSP endurecido em produção.
8. **Erros públicos — validado:** handlers globais e rotas sensíveis retornam mensagens estáveis; checagem externa sem sessão em `/applications/1`, `/preferences` e `/billing/document-export` retornou 401 sem stack trace.
9. **Rate limiting — concluído no ambiente atual:** além do limite local por IP/conta, `app/distributed_rate_limit.py` usa EVAL Lua atômico pelo REST do Upstash. A instância gratuita `agente-candidaturas-rate-limit` foi criada na região Oregon, próxima ao Render; `UPSTASH_REDIS_REST_URL` e o token rotacionado estão como secrets no Render, sem exposição no Git. `RATE_LIMIT_DISTRIBUTED_REQUIRED=true` está ativo tanto no serviço quanto no `render.yaml`; a indisponibilidade do Redis falha fechado. O primeiro teste em modo obrigatório expôs um formato incorreto de chamada REST e foi revertido imediatamente; o fix está no commit `5d736f2`, com 176 testes passando. Em produção, `/health` respondeu 200 e uma tentativa com credenciais sintéticas inválidas retornou 401 do Auth, confirmando que o Redis aceitou a chamada antes da autenticação. Prova concorrente entre múltiplas instâncias fica para quando houver escala horizontal. Sem plano pago.
10. **Senhas comprometidas — alternativa gratuita implementada e publicada:** a HIBP Pwned Passwords API é gratuita e sem chave; o backend envia só cinco caracteres do hash SHA-1 e adiciona padding. Cadastro e alteração bloqueiam senhas comprometidas; indisponibilidade, status inesperado ou resposta inválida retornam 503 e não liberam a senha. Os testes direcionados cobrem detecção, opt-out de teste, indisponibilidade, falha de rede e resposta malformada. A suíte oficial passou em **176 testes, 0 falhas**; o commit `1aa7da4` foi confirmado como Live no Render. A proteção nativa Supabase Pro permanece opcional, como defesa em profundidade; não é pendência de plano.
11. **Backup e recuperação — concluído, alternativa gratuita em operação:** o run diário do GitHub Actions guarda artifact cifrado por 30 dias. Em 20/09, o archive foi restaurado com sucesso num stack oficial local do Supabase descartável; `pg_restore --exit-on-error` concluiu e a verificação agregada encontrou 11 tabelas públicas, 527 linhas e 11 políticas RLS. Os 19 testes direcionados passaram, os recursos locais temporários foram removidos e o proprietário confirmou uma cópia segura da chave fora deste computador. **P0.11 concluído.** Sem upgrade do Supabase ou Render.
12. **Acessibilidade de modais — concluído:** em produção, os modais “Captar vaga” e “Preferências”, a tela de Segurança e o drawer de candidatura abriram com foco inicial, fecharam com `Esc`, mantiveram o ciclo de Tab dentro da superfície e devolveram o foco ao disparador. O commit `8cc11fa` foi publicado e validado no Render.
13. **Páginas legais públicas — validado:** `/termos` e `/privacidade` retornaram 200 sem sessão em produção, antes do cadastro, com headers de segurança ativos.

### P1 — resultado, proteção, LGPD, IA e monetização

1. Fechar o loop de resultado: **entregue e validado em produção** em `/applications/metrics` e em `Minhas candidaturas`, com escopo por usuário, segmentação por origem/canal, registro de retorno externo e vínculo do envio à versão de currículo/carta. A interface mostra o funil em `—` enquanto não há dados e só apresenta melhor canal/versão após cinco envios na mesma amostra. Falta acumular dados reais antes de qualquer promessa de melhoria de conversão.
2. Transformar proteção contra golpe em etapa explícita de risco: **entregue no código**. Faixa, score e evidências acompanham a candidatura; risco alto não libera o avanço pelo painel e risco duvidoso pede confirmação do usuário, registrada na linha do tempo. Falta ampliar a cobertura por origem e validar em produção.
3. **P1.3 publicado (`7cfa6aa`, Live no Render; correção `0d1523b` incluída nesta publicação):** heurísticas brasileiras versionadas como `br-rh-2`, códigos de motivo estáveis, preferências não verificáveis encaminhadas à revisão, atribuição de origem baseada em domínio declarado ou URL individual conhecida e split conservador de alertas; dados conflitantes de local/modalidade/salário/regime permanecem desconhecidos. Valores monetários sem contexto de salário não são inferidos como remuneração. 49 testes direcionados passam. Fixtures são sintéticas; ampliar validação com mensagens reais anonimizadas quando disponíveis.
4. Preparação de entrevista: **API owner-scoped entregue** em `/api/interviews/prep/{app_id}` e no detalhe da candidatura, com perguntas por gap, pontos fortes e orientação para responder com fatos comprovados. O simulador agora permite vincular uma candidatura e ajustar as perguntas; a avaliação Gemini no plano Pro está ligada e identifica claramente o feedback local quando a IA falha ou o plano não permite o recurso.
5. Implementar o acompanhamento da zona morta: **primeira versão entregue** em `/api/applications/followups` e em `Minhas candidaturas`, identificando candidaturas sem retorno há 7 dias e preparando uma mensagem para revisão; envio e registro do retorno continuam manuais.
6. **P1.6 implementado e testado:** a exportação JSON é owner-scoped; a exclusão definitiva remove todos os modelos do usuário incluindo `BillingSubscription`, mantém as contas alheias, faz purge local antes da remoção da identidade no Supabase, trata 404 como sucesso de retry e preserva acesso se falhar commit ou unlink de arquivo. **7 testes direcionados passam.** O proprietário confirmou a exclusão definitiva de uma conta de teste em produção; **P1.6 concluído**. Não há bucket de Storage ativo.
7. **Consentimento versionado entregue:** o cadastro exige os dois aceites, registra `terms_version`, `privacy_version` e `consented_at` no metadata enviado ao Supabase Auth; uma trilha imutável administrativa continua opcional.
8. **P1.8 — retenção concluída para o armazenamento atual:** documentos gerados são removidos por `DOCUMENT_RETENTION_DAYS`; a rotina periódica apaga mensagens processadas antigas e redige `raw_excerpt` após `RAW_DATA_RETENTION_DAYS` (padrão de 60 dias, mínimo de 30); temporários de OCR são removidos ao fim do processamento. Não há bucket Supabase nem prints/rascunhos persistidos, então a limpeza desses objetos só será necessária se Storage for adotado.
9. **P1.8 concluído para os fluxos atuais:** intake de texto/arquivo, Gmail e Outlook sanitizam conteúdo antes da persistência/análise; o contexto não confiável enviado ao Gemini é delimitado e testado. Regressões direcionadas cobrem markup hostil nos caminhos compartilhados e Outlook; qualquer novo conector ou prompt de IA deverá ganhar testes próprios. Não há limpeza de Storage pendente enquanto nenhum bucket/artefato persistido for adotado.
10. **P1.9 — persistência estruturada concluída para o intake atual:** parser de intake classifica CLT/PJ/MEI/estágio/temporário/freelance, retorna confiança de regime, modalidade e salário, preserva o texto salarial e os limites numéricos da fila à vaga promovida e mantém o salário oferecido separado das preferências do candidato. A cobertura específica por fonte e novos conectores permanece em P1.3/P2.2.
11. **Entrega pós-pagamento — P1.10:** `7bc618a` publica geração durável no servidor após webhook confirmado, além de recibo, persistência privada, histórico, download owner-scoped, e-mail dos dois DOCX, retenção, exclusão LGPD e retry de geração. Host, remetente, usuário, porta e TLS estão salvos no Render; as variáveis SMTP aparecem salvas; resta confirmar o endereço da conta e a entrega real em Gmail/Outlook.
12. Finalizar os textos legais com responsável, canal de contato, retenção e subprocessadores.
13. **Entregue:** sincronizar o card de onboarding com o perfil e as preferências reais, escondendo-o quando concluído e ajustando o CTA quando só o perfil estiver preenchido.
14. **Logout entregue no cabeçalho global:** subpáginas autenticadas agora exibem `Sair` por formulário POST; links legais continuam nas páginas públicas e no cadastro.
15. **Skeleton e proteção do formulário entregues:** o drawer de candidatura usa blocos animados durante a abertura, preservando textos para leitores de tela; `Limpar formulário` agora é uma ação neutra e confirma a perda quando existe rascunho ou prévia. O Banco de vagas foi reaberto em produção e exibiu o estado vazio/contadores normalmente, sem reproduzir o crash.
16. **Entregue:** landing explicita downloads avulsos de R$ 9,90 e inclui FAQ sobre candidatura manual, compatibilidade ATS e cancelamento conforme checkout; nenhuma prova social numérica foi inventada.
17. **P1.17 — primeiro acesso e polimento, 100%:** o tour guiado de três passos salva o progresso por conta, retoma ao voltar de currículo/preferências e só dispensa por ação explícita. Entrevistas e a seleção/vínculo da vaga no estúdio têm skeleton, `aria-busy`, retry e estados vazios/erro; todos respeitam movimento reduzido. `/perfil` permite editar experiências, competências, formação e idiomas e preserva dados não alterados nas reimportações. Hover dos botões, pulso do status conectado, feedback de upload/OCR, skeleton/retry no Banco de vagas/drawer e favicon SVG exclusivo da Candidatura Certa já estão publicados. `/candidaturas` mantém a lista quando dados auxiliares falham; `/configuracoes` só habilita salvar depois de receber preferências válidas e mostra retry em erro; `/seguranca` recupera falhas de rede/timeout e anuncia o estado acessivelmente. `cc1595e` ajustou o encaixe dos preços; `4f23938` conclui a revisão autenticada com launcher compacto, correção do layout OCR e remoção de cabeçalhos duplicados. Render confirmou `4f23938` **Live** e as telas autenticadas foram conferidas visualmente em produção.
18. **Suporte e ajuda contextual — widget e central publicados:** `/ajuda` é uma central pública, acessível antes do login e pelo menu de Ações do dashboard; o widget de chat carrega na landing e no shell autenticado e responde por catálogo revisado. Permanecem a validação da caixa de entrada de suporte, o teste de recebimento e a análise das dúvidas frequentes para evoluir a FAQ; as variáveis SMTP estão salvas.
19. **P1.19 — ciclo de vida por e-mail, 90% (estimativa):** follow-up, alertas de entrevista e de expiração usam transporte compartilhado, com preferência pela API Brevo e fallback SMTP TLS/SSL; mantêm opt-in, destinatário confirmado, outbox idempotente, retry e expurgo após 60 dias. Boas-vindas não será duplicada porque o Supabase já confirma a conta. Falta validar entrega real; os avisos de expiração também dependem de datas confiáveis nas fontes.
20. **P1.20 — governança de fontes, 40%:** as nove fontes escolhidas foram verificadas com documentação oficial e registradas individualmente; 0/9 têm autorização arquivada. O inventário dos 30 domínios ATS também está completo, com 0/30 autorizados. O código deny-by-default exige evidência, endpoint exato, direitos de coleta automática e exibição comercial por padrão, usos adicionais e revisão após 180 dias. Ainda faltam acordos/licenças dos proprietários para as fontes piloto; até lá, toda coleta automática permanece bloqueada.
21. **Estúdio de documentos ligado à fila:** o usuário pode escolher uma candidatura já captada ou preencher manualmente. A opção automática carrega cargo, empresa, modalidade, URL e descrição, reaproveita a candidatura existente e evita duplicidade; permanece como melhoria P1 do fluxo de documentos, sem alterar o gate de pagamento.
22. **P1.21 — preparação e avaliação de entrevistas, 90% (estimativa):** o simulador busca candidaturas do usuário autenticado, importa perguntas baseadas nos gaps e mantém perguntas gerais disponíveis. Respostas locais ou Gemini são identificadas; erros 402/503 permanecem explícitos, e textos do modelo são limitados e escapados. `58d286a`, `b7bfe46` e `8c604fd` estão **Live**. Dois testes Node cobrem reiniciar a simulação e trocar o contexto preservando rascunhos; os testes Python direcionados passaram e a suíte oficial completa passou em 327 testes. A variável `GEMINI_API_KEY` está presente no Render, mas ainda falta provar que é válida e exercitar o fluxo com uma conta Pro real.

### Próximo ciclo prático já classificado

1. **Gate de e-mail confirmado:** implementado no backend e conferido no Supabase (Email habilitado e `Confirm email` ligado); recebimento e reenvio operacional confirmados em Gmail e Outlook. Não há nova implementação P0 neste item.
2. **Idempotência de pagamentos:** assinatura e replay da compra existente foram validados em produção; **P0.5 concluído para a transação testada**. O código registra `order_nsu` e `payment_id` e permite apenas uma transição válida para `PAID`.
3. **P1.8 concluído para o modelo atual:** documentos, mensagens processadas, trechos brutos e temporários de OCR têm remoção automática; os caminhos atuais de captura/IA têm testes de sanitização. Nenhum bucket ou print/rascunho persistente está ativo; novas fontes exigem seus próprios testes.
4. **P1.6 — portabilidade/exclusão LGPD concluída:** código, testes e exclusão definitiva de uma conta de teste em produção confirmados. P1.10 segue como gate técnico de e-mail: inserir a senha SMTP diretamente no Render e testar recibo/documentos numa caixa confirmada.

### P2 — escala e diferenciação

1. **P2.1 — APIs autorizadas de vagas:** avaliar e integrar agregadores/parceiros somente após o gate P1.20, com licença, cobertura, custo, limites e atribuição documentados.
2. **P2.2 — Piloto de ATS públicos:** começar com duas ou três fontes públicas estáveis, como Gupy e portais de carreira, usando adaptadores pequenos e desligáveis; não iniciar por LinkedIn/Catho protegidos.
3. **P2.3 — Pipeline e orçamento por fonte:** consolidar fetcher, parser, deduplicador, enriquecedor e proveniência, com `external_id`/URL canônica/fingerprint, rate limit, backoff e limites de conteúdo.
4. **P2.4 — Workers e agendamento:** separar Gmail/Outlook, OCR/IA/documentos e ingestão de fontes em workers com filas, timeout, concorrência distribuída e retry controlado.
5. **P2.5 — Saúde dos conectores:** medir sucesso de busca, bloqueio, latência, parse válido e duplicidade por fonte; pausar e alertar quando a taxa cair abaixo do limite definido (referência inicial: 80%).
6. **P2.6 — Proxies e browser stealth condicionados:** avaliar rotação de saída e automação compatível somente para fontes autorizadas, com orçamento por domínio, limites de requisição, auditoria, respeito a robots/Termos e desligamento quando houver bloqueio ou queda de qualidade. Não contornar CAPTCHA nem controles de acesso.
7. **P2.7 — Configurar `candidaturacerta.com.br`:** domínio associado ao Render e CNAMEs raiz/www já configurados no Cloudflare. O Registro.br informa que os nameservers `frida.ns.cloudflare.com` e `todd.ns.cloudflare.com` ainda estão em transição; depois da propagação, falta verificar certificado/redirect e atualizar o endereço base e os callbacks de autenticação.
8. Adicionar Kanban de candidaturas e exportação CSV/Excel/JSON.
9. Criar extensão de navegador para captação autorizada.
10. Criar painel administrativo para acompanhar a operação e as métricas já instrumentadas.
11. Adicionar aprendizado baseado nos resultados das candidaturas depois que houver volume e atribuição confiáveis.

## Validações recentes

- Compilação de `app/auth.py`, `app/main.py` e `app/security.py` aprovada.
- 25 testes de autenticação e controles de segurança aprovados, incluindo cadastro/login/callback pendentes e redirecionamento de contas não confirmadas.
- Cabeçalhos de segurança confirmados no endpoint público `/health`.
- Deploy `e84fd60` confirmado como ativo no Render.
- Health check do Render e monitor externo UptimeRobot fazem parte da operação; credenciais e IDs dos monitores não são documentados por segurança.
- O domínio próprio `candidaturacerta.com.br` está associado ao Render e tem CNAMEs raiz/www configurados, mas a zona Cloudflare segue pendente durante a propagação dos nameservers. A produção continua acessível pelo hostname Render até concluir certificado, endereço base e callbacks. O e-mail de contato aguarda MX/provedor e teste real.
- A suíte completa foi reexecutada após a liberação pública das páginas legais, a remoção do fallback de pagamento legado, o CSP endurecido, o vínculo transacional dos exports, o fail-closed do webhook, a preservação de respostas HTTP no feedback global, a rotina segura de backup, a proteção do rascunho no estúdio, a atribuição de resultado, a porta de risco antes da candidatura, o filtro de alertas agrupados, a limpeza de assuntos de alerta, a preservação da origem da plataforma e o reconhecimento dos links por provedor: **139 testes aprovados em 4,56 s**, incluindo autenticação, sanitização HTML, normalização de títulos, parser de e-mail, regressão de rotas legais, bloqueio do fallback fora do Mercado Pago, rastreamento de canal/retorno/versão, isolamento da revisão de risco e carregamento dos scripts de segurança. O teste direcionado de RLS/IDOR também passou. A verificação de produção confirmou `/health`, `/termos`, `/privacidade`, `/dashboard`, rejeição 401 do webhook sem assinatura e o diálogo de confirmação ao limpar um formulário preenchido; 11 logins sintéticos acionaram 429 no limite configurado. O registro histórico de 59 testes ficou desatualizado porque novos testes foram adicionados.
- A suíte foi repetida nesta rodada com todas as dependências de `requirements.txt` resolvidas em ambiente isolado: **143 testes passaram, 0 falhas**, incluindo cobertura de desafio MFA trocado, código inválido, status baseado no usuário autenticado, login opcional com fator verificado, exportação publicada, mensagens seguras na fila e callbacks OAuth sem eco de detalhes do provedor.
- Nesta rodada, os testes direcionados de upload, armazenamento privado e OCR passaram (**11 testes**) e a suíte completa permaneceu em **143 testes aprovados, 0 falhas**. O fluxo MFA também passou a verificar automaticamente que os cookies do desafio usam `HttpOnly`, `SameSite=Lax` e expiração de 5 minutos.
- A reexecução direcionada de upload, armazenamento privado, OCR e fluxo integrado passou em **26 testes, 0 falhas**; a produção confirmou rejeição de arquivo inválido, preview de PDF válido e importação do currículo. A limpeza periódica de artefatos permanece em **P1.8**.
- **Registro histórico anterior ao replay de 20/09/2026:** a suíte direcionada do Mercado Pago passou em **12 testes**, cobrindo HMAC com janela de replay, rejeição sem assinatura/token, valor e moeda, transição `PAID` idempotente, replay conflitante, ausência do fallback InfinitePay e recibo sem duplicidade. Naquele checkpoint, checkout e replay real ainda aguardavam a transação controlada; o replay foi concluído depois, conforme a atualização operacional no início deste documento.
- O onboarding guiado do dashboard foi publicado com dispensa persistente, três etapas e ações de atalho; o teste de shell foi ampliado e a suíte completa passou para **144 testes aprovados, 0 falhas**. O JavaScript inline do dashboard também passou na verificação de sintaxe.
- A URL pública `/dashboard` retornou HTTP 200 e já contém o diálogo `guidedOnboardingDialog`, confirmando a propagação do onboarding guiado em produção.
- O polimento visual do dashboard foi validado com a suíte de shell, verificação de sintaxe JavaScript e a suíte completa em **144 testes aprovados, 0 falhas**; transições e o pulso do status são desativados para usuários que preferem movimento reduzido.
- A importação em `/curriculos` passou a indicar processamento com spinner, `aria-busy` e mensagem orientada à etapa; a sintaxe do script, os testes de shell e a suíte completa permaneceram aprovados (**144/144**).
- A Central de Ajuda pública foi publicada em `/ajuda`, incluída na lista de rotas sem autenticação e ligada ao menu de Ações do dashboard; o teste de shell confirma conteúdo sobre formatos, privacidade, pagamentos e recuperação de erros. A suíte completa passou para **145 testes aprovados, 0 falhas**.
- A verificação externa pós-deploy confirmou `/ajuda` com HTTP 200 e conteúdo de formatos, privacidade e pagamentos no Render; a rota continua pública para leitura antes do login.
- O resumo hierárquico da importação de currículo passou nos testes de shell, na verificação de sintaxe JavaScript e na suíte completa: **146 testes aprovados, 0 falhas**.
- O Render marcou o deploy do commit `34c47b4` como **Live**; `/health` continua retornando HTTP 200 após a publicação da tela de Currículos.
- O estúdio de documentos passou a sinalizar a geração assíncrona com botão ocupado, spinner e mensagem orientada; a suíte completa ficou em **147 testes aprovados, 0 falhas** e a sintaxe do script foi validada com `node --check`.
- As preferências de e-mail de ciclo de vida passaram na suíte direcionada e na suíte completa: **149 testes aprovados, 0 falhas**; valores fora da lista são normalizados para `daily` e as escolhas não disparam mensagens sem SMTP configurado.
- A tela publicada de Configurações mostrou os controles de frequência e categorias de aviso no Render após o deploy `bab87b5`; nenhuma mensagem foi enviada durante a validação.
- A correção dos alertas passou em `node --check` e na suíte completa: **150 testes aprovados, 0 falhas**; a validação local confirma que o script não recria controles, mantém o badge crítico separado e não contém `mailto` de suporte não configurado.
- O favicon SVG exclusivo da Candidatura Certa foi publicado no shell público, páginas legais e páginas autenticadas; o SVG foi validado como XML, `app/main.py` compilou e a suíte completa passou para **151 testes aprovados, 0 falhas**, incluindo 15 regressões de shell.
- A validação externa no Render confirmou HTTP acessível para `/static/favicon.svg` e a referência do favicon no dashboard publicado.
- O Banco de vagas passou a usar skeleton acessível durante a busca e no detalhe, com retry local e mensagem de recuperação quando a API falha; a suíte completa passou para **152 testes aprovados, 0 falhas**.
- Currículo e preferências passaram a expor carregamento e falhas de recuperação ao usuário, sem silenciar erros; a suíte completa passou para **153 testes aprovados, 0 falhas**.
- Candidaturas passaram a expor estado de carregamento, alerta de falha e retry local; o salvamento de preferências também trata falhas de rede. A suíte completa passou para **154 testes aprovados, 0 falhas**.
- Entrevistas passaram a expor estado de carregamento, alerta de falha e retry local; a suíte completa passou para **155 testes aprovados, 0 falhas**.
- Perfil e configurações passaram a tratar falhas de carregamento, foto e salvamento com mensagens acionáveis; a suíte completa passou para **156 testes aprovados, 0 falhas**.
- A cobertura de P0.1 foi ampliada para garantir que a exportação de privacidade também exclua compras de outro proprietário; a suíte oficial permaneceu em **143 testes aprovados, 0 falhas**.
- A fila deixou de devolver exceções internas em ações individuais e em lote: erros inesperados agora ficam apenas nos logs e chegam ao cliente como mensagem estável. O commit `9461640` cobre essa fronteira com regressão automatizada; o P0.8 permanece concluído para as rotas revisadas.
- Os callbacks OAuth do Gmail e Outlook agora registram o motivo técnico somente no log e devolvem mensagens públicas estáveis; a suíte cobre que detalhes enviados pelo provedor não chegam ao navegador.
- A exportação JSON foi validada em produção nas duas sessões autenticadas: o botão `Baixar meus dados` iniciou o download com mensagem de estado e cada sessão recebeu apenas o próprio escopo; o commit `9b0211d` publicou o listener que faltava no frontend.
- A API de exportação foi exercitada diretamente no contexto autenticado das duas sessões reais: a conta A recebeu HTTP 200 com apenas seu perfil, 1 vaga, 1 candidatura e 5 compras; a conta B recebeu HTTP 200 com perfil ausente e zero vagas, candidaturas e compras. Em seguida, a conta B tentou baixar o currículo e a carta associados à candidatura da conta A e recebeu HTTP 404 nos dois endpoints, sem conteúdo pessoal. P0.1 fica concluído para as rotas atuais.
- No painel do Render, as variáveis legadas `INFINITEPAY_EXPORT_PRICE_CENTS` e `INFINITEPAY_HANDLE` foram removidas e o novo deploy do commit `69d033d` ficou ativo; Mercado Pago permanece como o único checkout configurado.
- Registro histórico: nesta inspeção anterior, o ambiente não tinha variáveis `SMTP_*`. Atualização posterior de 21/09/2026: host, remetente, usuário, porta e TLS foram salvos; `SMTP_PASSWORD` foi salva posteriormente; permanece pendente o teste de entrega (**P1.10**).
- A suíte automatizada em `tests/` foi reexecutada após o endurecimento do CSP, do vínculo transacional dos exports, do fail-closed do webhook, do feedback de upload, da automação de backup, da proteção contra limpeza acidental e da atribuição de resultado: **129 testes aprovados em 5,11 s**, com 6 avisos de depreciação sem falhas. Os scripts legados na raiz continuam fora da suíte porque dependem de servidores locais em 8001/8002.
- A suíte oficial em `tests/` foi reexecutada no ambiente virtual local após a configuração do deploy atual: **168 testes aprovados, 0 falhas**, com apenas 6 avisos de depreciação. A execução ampla da raiz continua separada porque inclui scripts legados que tentam chamar servidores locais em 8001/8002 e também cópias de dependências em `tmp/`; esses erros de coleta não afetam a suíte oficial.
- Verificação pública pós-deploy: `/health`, `/termos` e `/privacidade` retornaram HTTP 200; `/api/profile` sem sessão retornou 401; e o webhook do Mercado Pago sem assinatura retornou 401. Nenhum pagamento, login ou alteração de dados foi executado.
- O indicador `Sistema conectado` da navegação global passou a usar ponto verde pulsante também nas subpáginas, com desativação para `prefers-reduced-motion`; o deploy `d50f3d0` ficou **Live** e a tela de Segurança foi conferida visualmente.
- A configuração de senha do Supabase foi reforçada com o requisito de letras maiúsculas/minúsculas, números e símbolos; o mínimo de 10 caracteres continua garantido pelo backend. O bloqueio de senhas vazadas usa a alternativa gratuita HIBP no backend e falha fechado; a proteção nativa Supabase Pro+ é defesa adicional opcional, fora do gate P0.
- O recibo idempotente do Mercado Pago foi validado com SMTP simulado e retry sem duplicação; os testes direcionados de webhook/fila passaram (**13 testes**).
- **Registro histórico anterior ao replay:** a suíte direcionada de webhook, armazenamento privado e exportação passou em **18 testes**, `/health` retornou 200 e o callback público de sucesso retornou 303 para `/criar-documentos?application_id=15&payment_status=approved`. A sessão com compra `PAID` existente exibiu os botões de currículo e carta gerados. Naquele checkpoint, ainda faltava a evidência externa do replay; ela foi concluída posteriormente em 20/09/2026, conforme a atualização operacional no início deste documento.
- A comparação salarial passou a usar limites estruturados da vaga quando disponíveis, com fallback para texto legado; Gmail também encaminha esses campos para a fila; a suíte completa ficou em **117 testes aprovados em 5,06 s**, com 6 avisos de depreciação sem falhas.
- A entrega P1.1 de métricas foi validada na suíte completa: **106 testes aprovados em 6,12 s**. O novo cenário confirma isolamento por usuário, contagem de candidaturas enviadas, entrevistas qualificadas e segmentação por origem; a interface de `Minhas candidaturas` exibe o funil sem alterar o fluxo existente.
- A primeira entrega de P1.2 foi validada na suíte completa: **107 testes aprovados em 5,68 s**. A fila passa a expor faixa/score de risco e sinais resumidos de golpe; o motor continua forçando descarte ou revisão conforme a banda, sem liberar automaticamente uma vaga suspeita.
- P1.3 recebeu versionamento explícito das heurísticas brasileiras: decisões manuais e alertas Gmail registram `br-rh-1` junto da versão do motor; a suíte completa permaneceu em **107 testes aprovados**.
- P1.4 recebeu um roteiro inicial de entrevista com escopo por usuário e perguntas derivadas dos gaps; a suíte completa passou para **109 testes aprovados em 9,85 s**.
- P1.5 recebeu o acompanhamento da zona morta com prazo de 7 dias, escopo por usuário e texto de follow-up sem envio automático; a suíte completa passou para **110 testes aprovados em 4,78 s**.
- P1.6 recebeu a primeira entrega de portabilidade: `/api/privacy/export` e botão de download em Segurança exportam perfil, vagas, candidaturas, eventos e pagamentos sem tokens; a suíte completa passou para **111 testes aprovados em 5,23 s**.
- A implantação do ciclo foi conferida no navegador: a landing exibe o FAQ e o preço avulso de R$ 9,90; `/seguranca` exibe exportação JSON e o botão global `Sair` após a propagação do deploy.
- A tela publicada de `Minhas candidaturas` foi conferida: o funil “Entrevistas qualificadas por 100 candidaturas enviadas” aparece com `—` sem amostra suficiente e mantém canal/versão ocultos até cinco envios, sem fabricar conclusão.
- Migração RLS de produção aplicada com `scripts/migrate_rls.py`: 11 tabelas com RLS ativo e uma política por tabela; `document_export_purchases_owner` confirmado como política `ALL`.
- Teste operacional com duas contas: conta A exibiu seus dados e conta B exibiu zero vagas/candidaturas; a tentativa de abrir diretamente `/dashboard?application_id=15` na conta B fechou o drawer sem revelar dados. A API autenticada também confirmou exportação owner-scoped e 404 para os downloads cruzados de currículo/carta. P0.1 fica concluído para as rotas atuais.
- Testes direcionados reexecutados nesta rodada: isolamento/RLS/arquivos cruzados (**3 aprovados**), validação de uploads por assinatura/estrutura (**4 aprovados**), fluxo MFA (**4 aprovados**) e assets estáticos públicos (**2 aprovados**). Essas evidências são locais; as validações externas do P0 continuam separadas por item.
- No avanço do P0.2, armazenamento privado/limpeza e intake de arquivos passaram (**5 testes**); em produção, um PDF falso e um DOCX falso foram rejeitados sem criar currículo. Após os commits `99c6019` e `f75c99c`, a tela passou a exibir somente a mensagem específica `O conteudo do arquivo nao corresponde ao formato informado.` e a região de status ficou acessível, sem o aviso global obsoleto. Nesta rodada, a produção rejeitou uma imagem inválida com HTTP 415 no upload de foto e HTTP 422 no preview de intake, sem persistir dados; a suíte agora também cobre a remoção dos temporários quando o OCR falha (**143 testes aprovados, 0 falhas**). Ainda falta validar uma imagem válida, o armazenamento privado e a limpeza periódica em operação.

## Auditoria do checklist de segurança e operação — 17/09/2026

| Item verificado | Evidência encontrada | Estado e prioridade |
| --- | --- | --- |
| Confirmação de e-mail | Gate no backend, tela pública de confirmação e reenvio limitado; no Supabase Auth, `Confirm email` está ligado, o provedor Email está habilitado, o Site URL aponta para o Render e há um redirect permitido para `/dashboard`; template usa `{{ .ConfirmationURL }}` | Código/configuração conferidos, regressão automatizada coberta e recebimento/reenvio confirmados em Gmail e Outlook em 18/09/2026; **P0.4 concluído** |
| Cookies e headers | Cookies `HttpOnly`, `Secure` configurável e `SameSite=Lax`; CSP nonceado sem `style-src-attr unsafe-inline`, HSTS, `nosniff`, `DENY` e políticas complementares | Código e header real validados em `/health`, `/termos`, `/privacidade` e `/dashboard` em produção |
| Rate limiting | Limites por IP/conta, testes de 429, Redis REST Upstash Free, segredo no Render, modo fail-closed ativo; `/health` 200 e login sintético 401 após a checagem distribuída | **P0.9 concluído** para o ambiente atual; validar tráfego simultâneo ao escalar |
| Isolamento/IDOR | Testes locais com dois usuários cobrem listagem, consulta, atualização e downloads; duas contas reais confirmaram a exportação JSON owner-scoped e receberam 404 nos downloads cruzados de currículo/carta | Código, testes locais, RLS e validação real das rotas atuais concluídos; manter a regressão em novas rotas |
| RLS e menor privilégio | Painel do Supabase confirma RLS ativo e uma política `ALL` para cada uma das 11 tabelas, incluindo `document_export_purchases_owner`; a política `applications_owner` exige relação com `jobs.owner_id = auth.uid()`; código cliente usa a chave publicável e o Render não exibe chave mestra | RLS aplicado e isolamento real por ID/exportação/download confirmado nas rotas atuais; não criar nova chave em **P0.6** |
| Segredos e TLS | Scanner do código versionado não encontrou chaves privadas, tokens hardcoded ou `SERVICE_ROLE_KEY`; painel mantém chaves mascaradas, `Enforce SSL` está ativo e o código usa `sslmode=require` | **P0.6 concluído** para o ambiente atual; repetir somente após novas integrações |
| Senhas comprometidas | Consulta k-anonimizada envia somente cinco caracteres do SHA-1; `PWNED_PASSWORD_CHECK` está ativo. Erro do serviço ou resposta malformada agora bloqueia a tentativa com resposta pública genérica. A proteção nativa Supabase Pro+ está desativada | Alternativa gratuita cobre o gate de produto em **P0.10**; upgrade nativo é opcional, não pendência |
| Uploads | Validador central confirma magic bytes, estrutura e decodificação de fotos; PDF/DOCX conferem assinatura/estrutura | Código e testes locais aprovados; validação operacional em **P0.2** e expurgo externo em **P1.8** |
| Arquivos temporários | OCR remove temporários ao terminar; documentos gerados usam diretório privado `0700` e limpeza periódica por idade; mensagens processadas antigas são removidas e `raw_excerpt` de filas antigas é redigido | Camada local implementada em **P1.8**; expurgo de Storage/rascunhos só após adoção de bucket |
| MFA | TOTP é voluntário por conta; enrollment/status/unenroll e challenge/verify exigem fator somente para quem o ativou; testes cobrem sessão e AAL2 | **P0.3 concluído no escopo de MFA opcional**; login manual de conta com TOTP é validação adicional; perda dos dispositivos não tem recuperação automática |
| Gmail/Outlook | Gmail usa `gmail.readonly`; refresh tokens são cifrados com Fernet; OAuth usa `state` assinado e expirável | Implementado; manter auditoria de configuração do provedor |
| XSS e prompt injection | Sanitizador central remove markup executável antes de persistir/analisar; prompt de entrevistas delimita dados não confiáveis e testes hostis verificam que tags/instruções não escapam | CSP e superfícies ativas endurecidos em **P0.7**; ampliar cobertura por origem permanece em **P1.8** |
| Webhook de pagamento | Rota pública do Mercado Pago valida `x-signature`/`x-request-id` com HMAC e janela de replay; sem Access Token retorna 503 e sem assinatura retorna 401; uma compra aprovada existente foi reenviada duas vezes e a segunda entrega confirmou idempotência | Código, configuração e replay controlado publicados; **P0.5 concluído para a transação testada** |
| Downloads e exportações | Rotas filtram a candidatura pelo usuário e exigem compra `PAID` do proprietário vinculada ao `application_id`; o checkout cria essa associação, o teste de candidatura sem compra retorna 402 e a exportação/download cruzado foi conferido com duas contas reais | Código, teste local e validação real das rotas atuais concluídos; manter cobertura para novas superfícies |
| Erros e informação interna | Handlers globais cobrem validação e exceções inesperadas; `/health` e IA retornam mensagens estáveis e registram detalhes apenas no log | Código e checagem externa sem sessão aprovados: rotas sensíveis retornaram 401 genérico sem stack trace |
| Tarefas pesadas | OCR, parsing, confirmação e busca externa usam threadpool com timeout total de 30 segundos; monitores ainda rodam no processo web e falta limite distribuído de concorrência/CPU | Código e testes locais aprovados; worker separado e limites distribuídos permanecem em **P2** |
| Backup e recuperação | Workflow gratuito ativo; archive cifrado restaurado num stack oficial do Supabase local; 11 tabelas, 527 linhas agregadas e 11 políticas RLS conferidas; cópia externa da chave confirmada pelo proprietário | P0.11 concluído |
| SQL injection | Consultas de negócio usam SQLAlchemy com parâmetros; SQL dinâmico encontrado no script de RLS usa apenas nomes de tabelas constantes do próprio código | Coberto na revisão atual; manter regra de não interpolar entrada do usuário |
| SSRF | `job_source_fetcher` rejeita credenciais, resolve DNS, bloqueia IPs não globais, revalida redirecionamentos e limita resposta | Coberto na revisão atual; manter testes de regressão |
| Termos, privacidade e consentimento | Páginas e checkbox existem; o cadastro agora exige os dois aceites e registra versões/data UTC no metadata do usuário | Implementado em **P1.7**; trilha imutável administrativa é melhoria posterior; textos legais em **P1.11** |
| Exportação e exclusão LGPD | Exportação JSON owner-scoped está disponível; a exclusão exige `EXCLUIR MINHA CONTA`, remove dados locais/documentos e identidade no Supabase; objetos em Storage ainda não existem | Código/testes em `d55eb0f` e exclusão definitiva de uma conta de teste em produção confirmada pelo proprietário; **P1.6 concluído** |
| Recibo e documentos por e-mail | Recibo idempotente; os dois DOCX são anexados somente após geração, com destinatário confirmado, outbox, retry e biblioteca protegida | **P1.10 entregue no código**; configuração SMTP salva, falta confirmar endereço e comprovar entrega real |
| InfinitePay | Removido do código, do Render e do exemplo de ambiente | Fora do escopo ativo; não validar nem recomendar como provedor |
| UptimeRobot | Monitor externo de disponibilidade/health check já faz parte da operação e está documentado; IDs e alertas ficam no painel externo | Concluído operacionalmente; conferir painel quando houver auditoria, sem recriar configuração |
| Suíte completa | Dependências de `requirements.txt` resolvidas em ambiente isolado; a suíte oficial em `tests/` executou **163 testes sem falhas** nesta rodada | **Concluído nesta verificação** |
| Acessibilidade dos modais | Script global registra disparador, foco inicial, retorno de foco, `aria-modal` e ciclo de Tab para `<dialog>` e modal customizado; produção confirmou captação, preferências, Segurança e drawer de candidatura | Validado em produção; foco inicial, `Esc`, retorno ao disparador e ciclo de Tab concluídos |

### Verificação direta do Supabase — 17/09/2026

- Ambiente de produção da Candidatura Certa, na região São Paulo, plano Free; o painel voltou a exibir **Healthy** após o carregamento e o Advisor informou não haver problemas de segurança ou performance. O identificador técnico do serviço Render permanece `agente-de-candidaturas`.
- Auth: confirmação de e-mail ligada; Site URL `https://agente-de-candidaturas.onrender.com`; redirect permitido para `https://agente-de-candidaturas.onrender.com/dashboard`; template de confirmação usa `{{ .ConfirmationURL }}`; SMTP customizado do Brevo está ativo. O recebimento e o reenvio foram confirmados em Gmail e Outlook em 18/09/2026.
- MFA: TOTP (aplicativo autenticador) habilitado; SMS MFA desabilitado. O enrollment continua opcional por conta; a correção local agora exige código no login apenas de contas com TOTP confirmado e ainda aguarda deploy/teste operacional.
- Auth nativo: limites de cadastro/login, refresh, verificação de token e envio de e-mail estão configurados no painel. A proteção contra CAPTCHA está desligada e a opção nativa de bloquear senhas vazadas aparece desabilitada; o aplicativo mantém a checagem k-anonimizada externa em **P0.10**.
- RLS: 11 tabelas do schema `public` aparecem com RLS ativo e uma política de proprietário para o papel `authenticated`. Não alterar nem recriar essas políticas; o próximo teste é tentar acesso cruzado por ID/download com duas contas reais.
- Storage: não há buckets criados no projeto. A aplicação continua usando armazenamento privado local; expurgo de objetos externos só entra quando um bucket for adotado em **P1.8**.
- Chaves: o painel separa chave publicável de chave secreta e mantém os valores mascarados. Não foi criada, revelada, copiada ou alterada nenhuma chave durante a auditoria.
- Banco: `Enforce SSL` está ativo no Supabase e a aplicação reconectou ao painel após o reinício; o código continua forçando `sslmode=require`.
- Security Advisor: após revogar `EXECUTE` de `PUBLIC`, `anon` e `authenticated` na função auxiliar `public.rls_auto_enable()`, a verificação efetiva retornou `false/false` para os dois papéis e os dois avisos de função desapareceram. O aviso nativo de proteção contra senhas vazadas permanece porque essa opção é Pro+, mas o bloqueio da aplicação usa a alternativa gratuita e fail-closed; não há upgrade pendente para **P0.10**.
- Backups: o plano Free não inclui backup diário/PITR nativo. A alternativa sem custo adicional é `pg_dump` diário no GitHub Actions com artefato cifrado por 30 dias. Na consulta original do painel em 17/09, a configuração ainda estava pendente; em 20/09, a secret, o run #4, o restore em Supabase local descartável e a cópia externa segura da chave foram confirmados. **P0.11 concluído.**

### Verificação externa do Render — 18/09/2026

- O deploy `cfb1c74` foi concluído com sucesso. O log do Render registrou `Application startup complete`, health checks `200` e serviço Live; a inicialização não executa mais a reflexão de colunas do PostgreSQL que causava timeout.
- `/health` retornou `200` com banco conectado; `/termos`, `/privacidade` e `/dashboard` retornaram `200` sem stack trace e com CSP nonceado, HSTS e `nosniff`.
- `/webhooks/mercadopago` retornou `401 Webhook Mercado Pago não autorizado.` para uma entrega sem assinatura, confirmando a rejeição pública após o deploy `75a72aa`.
- As rotas `/applications/1`, `/jobs/1`, `/vagas/1`, `/billing/document-export`, `/api/profile` e `/api/preferences` retornaram `401 Login necessario.` sem sessão. Isso confirma a barreira de autenticação, mas não substitui o teste IDOR com duas contas reais em **P0.1**.

### Validação de MFA em conta de teste — 18/09/2026 (comportamento anterior)

- A extensão do Chrome foi conectada ao Codex e permitiu controlar uma segunda sessão real, separada da conta A no navegador interno.
- Na conta B, o enrollment TOTP foi concluído na produção: a API gerou o fator, o código de seis dígitos foi verificado e a tela passou a indicar o autenticador como ativo. O MFA continua opcional porque `MFA_LOGIN_ENFORCE=false`.
- A API autenticada confirmou `enabled=true` e fator TOTP verificado na conta B; na conta A, confirmou `enabled=false` sem fatores. Uma chamada sem cookies de desafio ao endpoint de conclusão retornou HTTP 401 com a mensagem estável de desafio expirado, sem promover sessão.
- A conta com TOTP ativo entrou no dashboard sem prompt porque o ambiente mantém `MFA_LOGIN_ENFORCE=false`; o teste automatizado garante que um fator verificado não torna o login obrigatório enquanto essa política estiver desligada.
- A interface de Segurança tinha uma lacuna: após ativar o fator, não consultava `/auth/mfa/status` ao recarregar e não oferecia revogação, embora `DELETE /auth/mfa/{factor_id}` já existisse no backend. O script foi ajustado para carregar o estado real, mostrar erro sem falso “Não configurado” e permitir desativar o autenticador com confirmação.
- Após a primeira publicação, o status ainda consultava uma rota REST de fatores inexistente neste projeto. O backend passou a ler `factors` da resposta autenticada de `/auth/v1/user`, atualizando enrollment, status e login opcional sem depender do schema REST do banco; o deploy `de55ee4` foi confirmado como **Live** e a conta B exibiu `Ativo` com `Desativar autenticador`.
- O teste de ponta a ponta de login ficou pendente após a validação de 18/09 e é retomado na correção de 20/09 abaixo; a MFA continua opcional por conta, sem exigência global.

### Correção de MFA opt-in — 20/09/2026

- A regressão identificada era que `MFA_LOGIN_ENFORCE=false` permitia que até uma conta com TOTP confirmado entrasse sem código. O comportamento contrariava a escolha individual de ativar o segundo fator.
- O login agora consulta fatores verificados após a senha correta. Conta sem fator recebe sessão normal; conta com TOTP recebe apenas cookies temporários de desafio por cinco minutos e não ganha sessão até o código ser validado pelo Supabase. O limite de tentativas do desafio continua aplicado.
- `MFA_LOGIN_ENFORCE` foi removida da aplicação, do `.env.example` e do `render.yaml`; a MFA não é imposta a quem não a ativou. Os sete testes de `tests/test_mfa_flow.py` passaram. A correção foi publicada no Render pelo commit `9dfcaff`, já Live e saudável; falta validar um login real com TOTP numa conta de teste.

### Validação de acessibilidade do drawer — 18/09/2026

- O deploy `8cc11fa` foi confirmado como **Deployed** no Render.
- Em produção, a abertura da candidatura colocou o foco no botão `Fechar`; `Esc` fechou o drawer e devolveu o foco à linha que o abriu.
- Após 18 avanços de `Tab`, o foco permaneceu dentro dos controles do drawer, sem escapar para a navegação ou para o conteúdo atrás do overlay.

## Variáveis e segredos

Os valores reais não pertencem a este documento. Devem permanecer somente no painel do Render ou no ambiente local protegido:

- `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `DATABASE_URL`;
- `COOKIE_SECURE`, `AUTH_REQUIRED`, `APP_BASE_URL`;
- A antiga variável `MFA_LOGIN_ENFORCE` foi removida do código e do Blueprint; se ainda existir no painel do Render, é ignorada. A política é por conta: fator confirmado exige código no login, e conta sem fator segue sem MFA;
- `PWNED_PASSWORD_CHECK` (habilitado no Render para bloquear senhas presentes em vazamentos conhecidos);
- `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `OAUTH_STATE_SECRET`;
- `TOKEN_ENCRYPTION_KEY`;
- `GEMINI_API_KEY`;
- `MERCADOPAGO_WEBHOOK_SECRET` e credenciais do Mercado Pago.
- `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` e
  `RATE_LIMIT_DISTRIBUTED_REQUIRED` (rate limiting compartilhado; nunca expor
  o token ao cliente).

## Histórico recente de entregas

| Commit | Entrega |
| --- | --- |
| `5d736f2` | Corrige o comando EVAL enviado ao REST do Upstash; 176 testes aprovados e validação em modo obrigatório |
| `8dd0f36` | Gmail ignora alertas agrupados sem vagas individuais, evitando criar oportunidades artificiais; testes 132/132 |
| `9e01036` | Proteção contra golpes antes da candidatura: preserva sinais de risco, bloqueia risco alto e exige revisão auditável para vaga duvidosa; testes 131/131 |
| `fe2d92f` | Atribuição de candidatura por canal, retorno externo e versão de currículo/carta; proteção de amostra mínima no painel |
| `61a4ab1` | Regressão do painel para os controles de canal e retorno; suíte 129/129 |
| `4cddd50` | Ação neutra `Limpar formulário` com confirmação contra perda acidental, validada em produção; testes 128/128 |
| `2315bfc` | Remoção do segundo cabeçalho da tela de Segurança, validada em produção |
| `0f9f6e2` | Rotina segura e verificável de backup externo do Supabase Free |
| `f75c99c` | Feedback de upload acessível, sem status global obsoleto |
| `ca85397` | Remoção do aviso HTTP genérico duplicado |
| `99c6019` | Preservação da mensagem específica de validação de upload e correção visual da área de importação |
| `75a72aa` | Validação pós-deploy do webhook Mercado Pago e registro de configuração ativa |
| `f75f07f` | Webhook Mercado Pago fail-closed para Access Token ausente e assinatura inválida; testes 123/123 |
| `d7cf3c1` | Registro da limitação do plano Free para proteção nativa contra senhas vazadas |
| `3c6862e` | Validação de produção do Banco de vagas sem crash |
| `12e9c65` | Confirmação direta no Supabase de Email habilitado e `Confirm email` ativo |
| `579ee2c` | CSP endurecido sem `style-src-attr unsafe-inline` |
| `e293c8c` | Exports vinculados à candidatura e compra paga exata |
| `cad91fb` | Cabeçalhos de segurança e rate limiting de autenticação |
| `863f9e8` | Verificação de e-mail e portabilidade/exclusão agrupadas no planejamento P0/P1 |
| `66f5c7d` | Retenção, recibos, prompt injection e parsing de regime/modalidade/salário no planejamento |
| `2c05531` | Refinamento do backlog de segurança, logout, onboarding, XSS e webhooks |
| `f2d9a7c` | Roadmap de segurança, privacidade e LGPD |
| `b44daee` | Ações do dashboard, busca e paginação da fila |
| `6c00d98` | Estúdio de currículo e carta adaptados |
| `90aeccb` | Checkout de exportação via Mercado Pago |
| `4f00417` | Checkout de exportação via InfinitePay |
| `14b0e8f` | Checkout reserva a aba no clique, expõe erro no próprio bloco e estúdio reutiliza vaga captada; suíte 168/168 |
| `cef6302` | Preço avulso do checkout configurado em R$ 9,90 no Blueprint do Render |
| `602c0b4` | Retorno do Mercado Pago restaura a candidatura, aguarda webhook e gera currículo/carta pagos de forma idempotente; suíte 169/169 |

### Preparação dos cinco gates externos do P0 — 18/09/2026

- **E-mail:** recebimento e reenvio confirmados em Gmail e Outlook em
  18/09/2026; o roteiro permanece em `scripts/README_OPERACIONAL_P0.md` para
  regressão futura.
- **Mercado Pago:** o checkout e a assinatura/idempotência já estão no código;
  `scripts/replay_mercadopago_webhook.py` repete a mesma entrega assinada duas
  vezes usando um `payment_id` real, sem criar pagamento.
- **Rate limiting:** Redis REST compartilhado do Upstash Free está criado; os
  secrets foram configurados no Render, o modo obrigatório está ativo e o
  deploy `5d736f2` passou por `/health` 200 e tentativa sintética de login 401.
- **Senhas vazadas:** a checagem k-anonimizada continua ativa no aplicativo;
  a proteção nativa do Supabase permanece condicionada ao plano compatível.
- **Backup:** o script agora pode restaurar explicitamente em uma URL de banco
  descartável (`--restore-to`), sem expor a `DATABASE_URL` nos argumentos; o
  agendamento e a restauração real ainda precisam de ambiente externo.
- **E-mail:** o usuário confirmou recebimento e reenvio em Gmail e Outlook;
  este gate operacional foi encerrado.
- **Checkout/documentos:** o botão de liberação agora abre a aba do Mercado
  Pago no próprio gesto do clique, mostra o erro no bloco de pagamento e
  informa que o Pix aparece quando habilitado na conta Mercado Pago. O estúdio
  oferece preenchimento manual ou seleção de uma candidatura captada, reutiliza
  a candidatura vinculada e não cria uma vaga duplicada.
- O retorno do pagamento agora redireciona diretamente para o estúdio com a vaga vinculada;
  a confirmação exata da compra é consultada por candidatura, o webhook pode chegar depois
  do redirecionamento e a geração idempotente persiste os dois arquivos antes de mostrar os
  botões de download. Selecionar novamente uma candidatura já paga restaura o mesmo fluxo.
- A suíte oficial foi reexecutada após essas mudanças: **169 testes aprovados,
  0 falhas**, com 6 avisos de depreciação já conhecidos.

### Atualização operacional de backup — 19/09/2026

- `scripts/backup_supabase.py` criou um dump real do banco de produção em
  `backups/database/`; a validação de arquivo e o checksum SHA-256 passaram.
- A restauração em PostgreSQL 17 descartável, vinculado somente a `127.0.0.1`,
  recuperou 11 tabelas do schema `public`, 11 políticas RLS e aproximadamente
  527 linhas estimadas, sem exibir conteúdo pessoal.
- O teste local excluiu a extensão `supabase_vault` e os dados de
  `vault.secrets`, pois o PostgreSQL vanilla não fornece essa extensão. Não
  equivale a uma restauração integral; essa validação precisa de um projeto
  Supabase compatível.
- O dump foi criptografado com AES-256-GCM/RSA-OAEP após o teste, e o arquivo em
  texto puro e seu checksum foram apagados; a chave privada permanece em
  `backups/database/`, fora do Git e com ACL restrita. O workflow do GitHub
  Actions agenda a execução diária às 08:17 UTC e mantém artefatos cifrados por
  30 dias. Na data deste registro (19/09), a secret `SUPABASE_DATABASE_URL`
  ainda não estava cadastrada. Em 20/09/2026, a secret foi configurada e o run
  manual #4 concluiu com artifact cifrado de 374 KB. Ainda falta guardar a chave
  privada num gerenciador seguro externo e validar restauração integral.

### Alternativas sem planos pagos

- **Senhas comprometidas:** usar a HIBP gratuita em vez da opção nativa
  Supabase Pro+. A consulta por k-anonimato falha fechada e o commit `1aa7da4`
  foi confirmado Live no Render.
- **Rate limit distribuído:** Upstash Free já está configurado como backend
  compartilhado; URL/token permanecem secrets no Render e o modo fail-closed
  está alinhado no Blueprint. A validação cross-instance entra ao escalar.
- **Backups automáticos:** usar GitHub Actions em vez de Render Cron ou
  Supabase Pro/PITR. Agendamento, secret, cifra, execução, restauração local
  Supabase-compatível e cópia externa da chave foram confirmados; **P0.11 está
  concluído**.
- **Webhook Mercado Pago:** reutilizar a compra aprovada existente no teste de
  replay idempotente; não há nova cobrança nem plano adicional.

## Regra para continuar o projeto

Não liberar candidatura automática geral nem aceitar usuários pagantes antes de concluir o P0, testar o isolamento de dados e validar os webhooks. Não registrar senhas, tokens, chaves ou URLs privadas em commits, Markdown, logs ou conversas.

Toda nova recomendação deve ser comparada com esta fila antes de virar tarefa. Se já existir, atualiza-se o item correspondente; se for nova, entra na prioridade que corresponde ao risco e à dependência. Uma etapa só sai da fila depois de código, teste e validação operacional compatíveis com o seu status.


- **P2.1–P2.6 — alicerce de ingestão (21/09/2026, local):** adicionados o catálogo compartilhado `job_listings`, migração SQL com unicidade composta e leitura RLS de vagas ativas, importador JSON com gate fail-closed, limite/lease distribuído, sanitização e upsert idempotente, além da fila PostgreSQL `job_ingestion_tasks` com idempotência, lease de worker recuperável, até cinco tentativas com backoff e estado server-only. Há um comando worker (`scripts/run_job_ingestion_worker.py`) e um agendador horário (`scripts/schedule_job_ingestion.py`). **A base segue sem serviços Render configurados e sem fonte ativada.** O registro continua vazio (0/9 fontes), portanto nenhum acesso ou coleta foi executado. Autorização, feed e campos da fonte piloto, implantação do worker/agendador e monitorização real seguem pendentes.

### Protocolo para novas mensagens

Qualquer nova mensagem, print, documento ou sugestão relacionada ao produto passa primeiro por uma análise de impacto. O fluxo é:

1. identificar o que já existe e o que já foi concluído;
2. verificar se a sugestão duplica, altera ou bloqueia uma tarefa existente;
3. classificar a entrada em P0, P1 ou P2 conforme risco, dependência e valor;
4. atualizar esta fila e o status correspondente;
5. continuar pelo próximo item executável da fila, sem interromper o restante do plano para tratar somente a última mensagem.

Quando a etapa depender de uma conta, credencial, painel externo ou validação manual, ela permanece marcada como bloqueio externo e a solicitação de acesso é feita apenas nesse ponto.
- **P1.22 — Copiloto: matriz de portais (1394cd6, Render Live):** a versão publicada adiciona um dicionário modular de seletores profissionais para Gupy, Vagas.com, InfoJobs, Catho, Sólides e Empregos.com.br, com setter nativo e eventos para campos React e fallback por rótulos. Campos sensíveis, perguntas abertas, consentimento, uploads e envio final continuam fora do escopo; listas suspensas personalizadas ficam para revisão manual. LinkedIn, Glassdoor e Jobbol continuam bloqueados pela política de automação. `16/16` testes Node, sintaxe JavaScript e `git diff --check` passaram; o ZIP de 18 arquivos está publicado.

- **P1.10/P1.21 — evidência de ambiente e conta (21/09/2026):** a lista do Render confirma `SMTP_PASSWORD`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USE_TLS`, `SMTP_USERNAME`, `GEMINI_API_KEY`, Mercado Pago e Upstash salvos; `BREVO_API_KEY` não aparece, portanto o fallback HTTPS da Brevo ainda não pode ser ativado. A conta autenticada disponível no site foi conferida em `/configuracoes` como plano Essencial, sem executar upgrade ou cobrança; o teste Gemini/Pro segue corretamente pendente de uma conta Pro.
### Atualização operacional — 22/09/2026

- **P1.10 — chave Brevo e publicação:** o painel do Render mostra `BREVO_API_KEY` salva e mascarada, sem revelar o valor. O deploy da mudança `5a5eb0d` aparece como **Live** no Render. A rota de histórico em `/curriculos` oferece “Tentar enviar por e-mail” para documentos que ainda não foram enviados; o botão envia o currículo e a carta ao e-mail confirmado da própria conta. Nenhuma tentativa foi acionada nesta validação; a confirmação na caixa do proprietário continua pendente.
- **P1.11 — textos legais:** a Política de Privacidade e os Termos públicos foram lidos em produção e exibem a identidade e os canais de contato do controlador informados pelo proprietário. Os Termos agora descrevem corretamente o provedor transacional configurado (Brevo ou SMTP).
- **P1.19 — transporte dos alertas (5a5eb0d, Render Live):** follow-up, entrevista e expiração usam transporte compartilhado com preferência pela API Brevo e fallback SMTP TLS/SSL. A indicação `lifecycle_email_configured` também reconhece Brevo sem SMTP. Os três testes de integração passaram.
- **P1.18 — resposta pública de planos:** o teste ponta a ponta encontrou uma frase antiga sobre a inclusão do e-book; a base oficial e a resposta curada foram corrigidas para refletir os planos atuais. Os 12 testes do chat passaram.
- **Testes focados:** 56 testes de transporte, alertas, biblioteca de documentos e candidatura por e-mail passaram localmente; nenhum e-mail real foi enviado por esta alteração. O Render Shell não está disponível no plano Free, então a confirmação final de entrega deve ser feita pelo botão de reenvio no histórico e pela caixa de entrada/spam.
