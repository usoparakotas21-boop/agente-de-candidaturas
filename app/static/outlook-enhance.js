(function(){const rows=document.querySelectorAll('.integration');if(rows.length<2)return;const row=rows[1],badge=row.querySelector('.badge');const a=document.createElement('a');a.className='secondary';a.href='/auth/outlook/start';a.textContent='Conectar';row.appendChild(a);fetch('/auth/outlook/status').then(r=>r.json()).then(x=>{if(!badge)return;badge.textContent=x.connected?'Conectado'+(x.email?' · '+x.email:''):(x.configured?'Não conectado':'Indisponível');badge.classList.toggle('integration-connected',!!x.connected);badge.classList.toggle('integration-disconnected',!x.connected)}).catch(()=>{if(badge)badge.textContent='Indisponível'})})();
