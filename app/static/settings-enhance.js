(function(){function focusHash(){const id=location.hash.slice(1);if(!id)return;const target=document.getElementById(id);if(target){setTimeout(()=>target.scrollIntoView({behavior:'smooth',block:'start'}),80)}document.querySelectorAll('[data-section]').forEach(x=>x.classList.toggle('active',x.dataset.section===id))}function status(){const el=document.getElementById('gmailStatus');if(!el)return;fetch('/auth/gmail/status').then(r=>r.json()).then(x=>{el.textContent=x.connected?('Conectado'+(x.email?' · '+x.email:'')):'Não conectado';el.style.color=x.connected?'#16794b':'#a15c00';el.style.background=x.connected?'#e6f7ef':'#fff5df'}).catch(()=>{el.textContent='Indisponível'})}window.addEventListener('hashchange',focusHash);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{focusHash();status()});else{focusHash();status()}})();
