(function () {
  const qs = Array.from(document.querySelectorAll('.question')), next = document.querySelector('#next'), back = document.querySelector('#back');
  if (!next || !qs.length) return;
  let i = 0; const answers = [];
  qs.forEach(q => { const a = q.querySelector('textarea'); if (!a) return; a.maxLength = 1200; const c = document.createElement('div'); c.className = 'char-counter'; a.insertAdjacentElement('afterend', c); const u = () => { c.textContent = `${a.value.length}/1200 caracteres · recomendamos pelo menos 40`; c.className = 'char-counter' + (a.value && a.value.length < 40 ? ' short' : ''); }; a.addEventListener('input', u); u(); });
  const local = text => { const clean = text.trim(), words = clean.split(/\s+/).filter(Boolean).length, letters = clean.replace(/[^a-záàâãéêíóôõúç]/gi, '').toLowerCase(), unique = new Set(letters).size; if (clean.length < 40 || words < 7) return {score:0,title:'Resposta insuficiente',msg:'Escreva pelo menos 40 caracteres com uma ideia completa e contexto.'}; if (letters.length > 12 && (unique / letters.length < .28 || !/[aeiouáàâãéêíóôõú]/i.test(letters))) return {score:0,title:'Resposta inválida',msg:'Parece uma sequência de letras. Conte uma situação real para receber um feedback útil.'}; let score=45; if(words>=25)score+=15; if(words>=55)score+=10; if(/(exemplo|projeto|empresa|equipe|situação|problema|resultado|liderei)/i.test(clean))score+=10; if(/\d|%|reduz|aument|entreg|impact|resultado|prazo/i.test(clean))score+=12; if(/(vaga|cargo|empresa|posição|oportunidade|contribu)/i.test(clean))score+=8; return {score,title:score>=80?'Resposta consistente':score>=60?'Pode ser fortalecida':'Resposta genérica',msg:score>=80?'Boa estrutura, evidências e conexão com a oportunidade.':'Inclua situação, ação e resultado mensurável.'}; };
  const paint = (q,r) => { const b=q.querySelector('.feedback'); b.className='feedback show '+(r.score>=80?'good':r.score>=60?'warn':'bad'); b.innerHTML='<strong>'+r.title+' · '+r.score+'/100</strong>'+(r.msg||'')+(r.strengths?.length?'<br><br><b>Pontos fortes:</b> '+r.strengths.join(' · '):'')+(r.improvements?.length?'<br><b>Melhore:</b> '+r.improvements.join(' · '):'')+(r.rewritten?'<br><br><b>Versão sugerida:</b> '+r.rewritten:''); };
  const render = () => { qs.forEach((q,n)=>q.classList.toggle('active',n===i)); const bar=document.querySelector('#bar'); if(bar)bar.style.width=((i+1)/qs.length*100)+'%'; if(back){back.hidden=false;back.disabled=i===0;} };
  const ask = async (q,text) => { const basic=local(text); if(!basic.score)return basic; try { const res=await fetch('/api/interviews/evaluate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q.querySelector('h2').textContent,answer:text})}); if(res.ok)return await res.json(); } catch (_) {} return basic; };
  const finish = () => { document.querySelector('#session').style.display='none'; const out=document.querySelector('#result'); out.classList.add('show'); const avg=Math.round(answers.reduce((a,r)=>a+r.score,0)/answers.length); document.querySelector('#score').textContent=avg; document.querySelector('#rows').innerHTML=answers.map((r,n)=>'<div class="result-row"><strong>'+qs[n].dataset.title+' — '+r.score+'/100</strong><span>'+(r.msg||r.title)+'</span></div>').join(''); const note=out.querySelector('.note'); if(note)note.textContent=answers.some(r=>r.provider)?'Análise feita pela mini IA Gemini, hospedada com segurança no backend.':'Análise estruturada disponível. A mini IA será usada quando a chave estiver ativa no Render.'; };
  document.addEventListener('click',async ev=>{if(ev.target!==next)return;ev.preventDefault();ev.stopImmediatePropagation();const q=qs[i],text=q.querySelector('textarea').value.trim();next.disabled=true;next.textContent='Analisando…';const r=await ask(q,text);paint(q,r);next.disabled=false;next.textContent='Avaliar resposta →';if(!r.score)return;answers[i]=r;if(i===qs.length-1)return finish();i++;render();},true);
  if(back)back.addEventListener('click',()=>{if(i>0){i--;render();}}); render();
})();
